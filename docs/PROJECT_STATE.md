# Project State

> **Status:** In Review
> **Last updated:** 2026-08-22
> **Related requirements:** All
> **Related ADRs:** ADR-0001–ADR-0014
> **Open questions:** OQ-001–OQ-050
> **Dependencies:** M0.2 evidence and shop review
> **Supersedes:** None

- **Current phase:** Phase 0 — Discovery and Validation
- **Current milestone:** M0.2 evidence and decision closure; TASK-001 and TASK-002 are validated on three OSes, while TASK-003 and TASK-004 are In Progress
- **Product readiness:** Pre-alpha engineering foundation. A bounded Apple-Silicon internal developer GUI runs end to end; Ubuntu 24.04 x86_64 and Windows Server 2022 x64 pass equivalent configured headless desktop-host smokes against separately assembled, host-verified pinned OCCT runtimes; and Ubuntu 22.04 x86_64 passes an unsigned extracted Debian package containing the verified native runtime, real retained-session STEP-to-USD-702 analysis, and bounded virtual-display launch. Nothing is persisted; no Windows window/package was exercised; the Debian package has no interactive dialog/accessibility/physical-display/install/signing/legal acceptance; and supported importer/release acceptance remain absent
- **Current branch:** `main`; prior TASK-002/TASK-003 branch histories are consolidated and obsolete local/remote task branches are deleted
- **Repository visibility:** Public as of 2026-08-12 so hosted CI can continue. The public source and workflow boundary contains only project code/documentation and sixteen governed project-authored synthetic CAD fixtures: three STL, ten 3MF, and three STEP files. The binary STL and ten 3MF packages are deterministic encodings derived from the public ASCII cube; private shop/customer/controlled data, credentials, signing material, and private validation artifacts remain prohibited from the repository and public CI
- **Last completed task/checkpoint:** The current TASK-004 slice advances to `partprobe-3mf-spike-v4` with persisted `FIX-MESH-013`: a two-link linear component chain with complete ordered ID/reference/transform provenance, explicit object/component ceilings, leaf-to-build transform application, and analytic canonical measurements. Branching, unions, multiple references per component object, non-immediate references, extra objects/items, and singular/reflected transforms still fail closed. Three-OS Rust CI run 32613812143 passes the preceding metadata checkpoint at exact commit `969def9`; run 32613154817 passes the persisted Core-unit corpus at `3f6e6bf`, and full native Linux package run 32610311910 passes the path-free exact geometry-label correction at `1177e10`. These checkpoints do not establish general component DAGs/unions, object/item/vendor metadata, independent representative geometry, mesh worker/desktop integration, a supported importer, or release approval. See `06-quality/linux-desktop-package-validation.md` and `06-quality/task-004-validation.md`
- **Work in progress:** TASK-004 now has bounded ASCII/binary STL and direct/linear-component-chain 3MF comparison slices with thirteen governed mesh fixtures, including persisted evidence for every Core unit/default and bounded well-known model metadata presence. Broader binary/malformed cases, branching component graphs/unions, object/item/vendor metadata and adversarial 3MF coverage, self-intersection/confidence policy, mesh worker/desktop integration, broader portal/Orca/accessibility, installation, signing, physical-display behavior, GUI-1 fixture review, representative corpus evidence, interactive Windows evidence, Windows/macOS package integration, macOS/Windows parser egress denial, general filesystem containment, legal/SBOM work, and remaining TASK-003 resource containment remain open. The GUI-4/GUI-5 slice is session-only, with no durable shop library, save/reopen, supported importer, viewer, signed package, or production approval
- **Next task:** Continue TASK-004 through adversarial branching/union/component-reference and object/item/vendor-metadata cases, additional binary/malformed cases, self-intersection/confidence policy, and typed worker/desktop integration. Then add interactive Windows evidence and continue macOS/Windows parser egress denial, general filesystem sandboxing/hard storage quotas, hard macOS memory, and hostile macOS descendant containment. In parallel, obtain GUI-1 fixture review. TASK-005 durable shop-owned rate/model-review UX and TASK-006 persistence remain after these bounded gates. No user input is needed for the immediate developer engineering slice; shop/security/legal review is still required at its named gates
- **Build status:** Exact OCCT `V8_0_0` commit `d3056ef80c9668f395da40f5fd7be186cae4501f` constructs on evidenced Apple Silicon, Ubuntu 24.04 x86_64, and Windows Server 2022/Visual Studio 2022 x64 configurations. Native Linux run 31555774851 passes in 53m39s at `34ce137`; native Windows run 31613259785 passes in 35m12s at `5869ac4`. Both assemble/re-verify new runtimes, inspect 24 binaries/23 OCCT dependency families, pass the configured STEP-to-USD-702 desktop-host smoke, and upload no developer binaries
- **Test status:** The current local macOS workspace passes 165 runtime tests and one compile-fail doctest, strict workspace/native-host/WASM Clippy, offline frontend construction, and 64 Python tooling tests. Seven STL and eleven 3MF tests cover thirteen governed mesh fixtures, including persisted packages for all six Core units/default, bounded model metadata presence/redaction, retained direct/component/build IDs and ordered transforms, bounded linear leaf-to-build chain order, shared topology/measurements, archive/XML/entity/object/component/metadata/compression limits, Core ID/reference rules, branch/non-immediate-reference rejection, unsafe relationships, unsupported structures/extensions/material attributes and singular/reflected transforms, malformed/non-finite input, and sanitized failures; ten tooling tests pin binary/3MF reproduction. Focused package-location work passes 4 contract, 11 UI, and 19 desktop-host tests with two native smokes ignored until explicitly configured. Three-OS Rust CI run 32613154817 passes the unit-corpus checkpoint on macOS, Ubuntu, and Windows at `3f6e6bf`; run 32607101403 passes the short Linux feature-off exact-payload interaction gate; run 31613259785 adds configured Windows native authority; and full native package run 32610311910 passes the corrected Ubuntu exact selection/analysis evidence gate at `1177e10`
- **Active technical debt:** The GUI has only ephemeral developer-session rate entry, not guided governance or persistence; pinned ISO 4217 registry validation, nonterminating rational-division rounding, rounding increments beyond decimal scale, and graph-wide evaluation/migration remain pending; canonical JSON remains an internal spike contract; mesh comparison uses exact floating-point vertex identity and does not yet cover binary attribute/color extensions, 3MF branching/DAG/union semantics, object/item/vendor metadata, self-intersections, a reviewed confidence ceiling, or desktop integration; supported STEP/STL/3MF corpora and representative machining fixtures remain pending
- **Active blockers:** No shop rates block TASK-002 mechanics because production libraries start empty and tests are synthetic; TASK-007/M0.2 still requires real category/policy/calibration review, and ADR acceptance plus broader production work still require shop/security/legal inputs and representative fixtures
- **Open architecture decisions:** ADR-0001 through ADR-0014 are In Review
- **Supported model formats:** None supported for production. Optional Apple-Silicon, Ubuntu-x86_64, and Windows-x64 STEP/OCCT developer spikes plus bounded path-free ASCII/binary STL and direct/linear-component-chain/model-metadata 3MF comparison analyzers handle governed synthetic evidence only. First-class plan: STEP, STL, 3MF; experimental plan: IGES, OBJ
- **Geometry capabilities:** Kernel-neutral contracts, bounded control schema v2, asset-transport manifest v2, explicit verified-private-copy selection, exact Unix descriptor and Windows HANDLE direct delivery with unrelated-resource exclusion proof, worker-side identity/type/hash/length/quota verification, immutable byte-stream adapter input, cooperative acknowledgement with forced termination, Linux `x86_64`/`aarch64` parser socket/process syscall denial, deny-by-default authorization/audit seams, root-identity-bound capability resolution, a consumed already-open grant, private per-job staging, immutable output claiming, poll/post-exit aggregate workspace-output inspection and cleanup, governed storage ports, and a mandatory worker resource profile are implemented. TASK-004 analyzes bounded already-authorized ASCII/exactly-framed binary STL into unresolved-unit mesh evidence and validates bounded in-memory 3MF ZIP/OPC packages with explicit/default units, bounded well-known model metadata presence, either one direct mesh/build item or a bounded linear chain whose component objects each reference the immediately preceding object once, ordered leaf-to-build component transforms, complete chain provenance, and canonical millimetre measurements. Persisted deterministic packages now cover all six Core unit declarations, the normative omitted-unit default, one model-metadata layout, and a two-link linear chain. Metadata evidence contains only counts and an explicit not-interpreted warning. All three format paths share triangle bounds, area, manifold, watertight, winding, and conditional volume/centroid logic. Branching, multiple references per object, non-immediate references, unions, object/item/vendor metadata, reflected/singular transforms, and extra objects/items fail visibly. GUI-4 intake can compute a size-bounded SHA-256 fingerprint only after authorization/audit and rewind the same open grant for worker consumption; worker verification remains independent. The provisional native output has a validated schema and decoder that binds reference and source hash. Configured Ubuntu x86_64 and Windows x64 evidence proves the pinned native adapter through the real desktop-host service; only Linux applies the parser syscall boundary. The Ubuntu Debian checkpoint additionally proves extracted package-resource runtime verification and real retained-session analysis. Broader branching/union/metadata and binary/malformed evidence, mesh desktop integration, general filesystem containment, hard storage quotas, hard macOS memory, macOS/Windows parser egress denial, hostile macOS descendants, Windows/macOS package integration, interactive Linux/Windows acceptance, corpus review, and production support remain open
- **Runtime capabilities:** None implemented. Six estimation levels and validation structure documented only
- **Advanced capabilities:** None implemented. Route optimization, capacity/opportunity, uncertainty, revision/PMI/coverage, feature allocations, learning, CAM reconciliation, availability, sourcing, and bid priority are planning artifacts only
- **Current fixtures:** `FIX-MESH-001/002` cover closed/open synthetic ASCII STL cubes; `FIX-MESH-003` is the reproducible binary encoding of the closed cube; `FIX-MESH-004` is the reproducible explicit-centimetre 3MF cube with a translated build item; `FIX-MESH-005` references that mesh through a separate scaled/translated component object before a translated build item and has analytic 20 × 10 × 10 mm, 1,000 mm², 2,000 mm³, `(60, 75, 95)` mm ground truth; `FIX-MESH-006`–`011` encode the same governed 10 mm cube in explicit micron, millimetre, metre, inch, and foot units plus omitted-unit default millimetres; `FIX-MESH-012` adds three public well-known model metadata entries while retaining only entry/preservation counts and unchanged canonical measurements; `FIX-MESH-013` adds a two-link linear component chain with analytic 20 × 30 × 10 mm, 2,200 mm², 6,000 mm³, `(70, 125, 115)` mm ground truth and retained ordered link evidence; `FIX-STEP-001` is a deterministic OCCT-generated AP214 10 mm cube; `FIX-STEP-002` contains an intentionally invalid entity; `FIX-STEP-003` is a manually authored AP214 faceted rectangular prism with independent analytic 12 × 8 × 5 mm ground truth, 392 mm² area, 480 mm³ volume, and `(6, 4, 2.5)` mm centroid. All hashes reproduce and native measurements pass; formal geometry/security review, broader CAD-tool independence, and licensing remain open
- **Calculation capabilities:** CALC-001/003/005 and CALC-007–018 foundations, with CALC-009 limited to exact division; empty configurable rate cards; scoped/effective/approved resolution; unavailable/blocked ambiguity states; versioned pricing/rounding; synthetic EX-01/03/12 exact itemized traces; pinned replay; typed money/currency/units; graph validation; value states; provenance/version wrappers; canonical snapshots; and a headless session-only application composition over reviewed provisional geometry/manual inputs/rates/policy are implemented as a reversible spike
- **Nearest testable GUI:** It exists now as the bounded GUI-5 Apple-Silicon developer slice: local STEP selection, real OCCT geometry facts, explicit review/manual/rate/pricing entry, and a deterministic trace run in the actual unsigned app. The worker and OCCT closure stay together in one runtime root that the desktop verifies at startup; Linux now proves that same root can be carried in an extracted unsigned Debian package. It still is not a convenient installable product: the Apple-Silicon app uses explicit developer configuration, data disappears on exit, only governed synthetic STEP evidence is validated, and there is no 3D viewport. A visible viewer still adds roughly two or three checkpoints
- **Files likely next:** TASK-004 adversarial branching/union/component-reference and object-item-vendor-metadata cases, binary/malformed cases, self-intersection/confidence policy, and worker/desktop contracts; GUI-1 review evidence; TASK-003 interactive Linux/Windows portal/dialog/accessibility, Windows/macOS package integration, application signing, dynamic-link/SBOM/legal evidence, macOS/Windows parser egress, general filesystem and remaining resource containment; TASK-005 durable rate/model-review UX; and TASK-006 persistence. The verified runbook and evidence are in `apps/estimator-desktop/README.md`, `06-quality/task-003-validation.md`, `06-quality/task-004-validation.md`, `06-quality/gui-5-validation.md`, and `06-quality/linux-desktop-package-validation.md`

## Commands currently available

```sh
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace --all-targets --locked
cargo test --workspace --doc --locked
cargo clippy -p partprobe-estimator-desktop-ui --target wasm32-unknown-unknown --locked -- -D warnings
cargo clippy -p partprobe-estimator-desktop --features desktop-host --all-targets --locked -- -D warnings
(cd apps/estimator-desktop && trunk build --release --locked true --offline true)
python3 scripts/check_planning.py
python3 scripts/hash_fixtures.py
python3 scripts/generate_3mf_fixture.py
python3 scripts/tests/test_native_tooling.py
python3 scripts/build_occt.py --source /approved/local/occt-source --build /private/tmp/partprobe-occt-build --install /private/tmp/partprobe-occt-install
python3 scripts/assemble_native_runtime.py assemble --occt-root /approved/local/occt-install --worker /approved/local/partprobe-geometry-worker --build-manifest /approved/local/occt-build/partprobe-occt-build-manifest.json --output /approved/local/partprobe-native-runtime
python3 scripts/assemble_native_runtime.py verify --runtime-root /approved/local/partprobe-native-runtime
python3 scripts/verify_native_runtime_links.py --runtime-root /approved/local/partprobe-native-runtime  # Linux
python3 scripts/verify_native_runtime_links.py --runtime-root C:/approved/runtime --dependency-tool C:/approved/VS/bin/dumpbin.exe  # Windows
PARTPROBE_NATIVE_RUNTIME=/approved/local/partprobe-native-runtime PARTPROBE_GEOMETRY_WORKSPACE=/private/tmp/partprobe-gui-worker cargo test -p partprobe-estimator-desktop --features desktop-host gui5_configured_worker_runs_real_step_through_retained_estimate_session --locked -- --ignored --nocapture
git diff --check
git status --short --branch
```

## Read these first

1. [AGENTS.md](../AGENTS.md)
2. [Documentation index](INDEX.md)
3. [Decision log](08-decisions/decision-log.md)
4. [Requirements matrix](03-requirements/requirements-matrix.md)
5. The requirement, ADR, architecture, domain, UX, and quality files relevant to the task
