# Project State

> **Status:** In Review
> **Last updated:** 2026-09-10
> **Related requirements:** All
> **Related ADRs:** ADR-0001–ADR-0014
> **Open questions:** OQ-001–OQ-050
> **Dependencies:** M0.2 evidence and shop review
> **Supersedes:** None

- **Current phase:** Phase 0 — Discovery and Validation
- **Current milestone:** M0.2 evidence and decision closure; TASK-001 and TASK-002 are validated on three OSes, while TASK-003 and TASK-004 are In Progress
- **Product readiness:** Pre-alpha engineering foundation. A bounded Apple-Silicon internal developer GUI runs end to end, and one local unsigned arm64 `.app` contains a manifest-verified OCCT 8.0.0/worker runtime and passes the packaged-resource STEP-to-USD-702 smoke without an external runtime path. Ubuntu and Windows pass configured headless desktop-host smokes; Ubuntu also passes an unsigned extracted Debian package. USE-2 now has schemas v1-v4 for rate/pricing, starter resources, multi-entry catalog/selection, and append-only authorization decisions; separate governed draft and activation services; contract-v7 path-free review; contract-v8 native-owned activation; and contract-v9 native-owned complete bounded catalog-draft save mapping. The exact nine-command host surfaces match. Settings now presents catalog records through a compact five-category search/list/detail browser. Ordinary startup has no authenticated actor, so both catalog calls return explicit native-identity unavailability, activation remains deny-all, and the WebView invokes neither. Models and estimates remain session-only, saved resources are not calculation inputs, and catalog editing is not enabled. The macOS app is unsigned/unnotarized and not approved for OCCT redistribution; supported importer and release acceptance remain absent
- **Current branch:** `main`; prior TASK-002/TASK-003 branch histories are consolidated and obsolete local/remote task branches are deleted
- **Repository visibility:** Public as of 2026-08-12 so hosted CI can continue. The public source and workflow boundary contains only project code/documentation and sixty-nine governed project-authored synthetic CAD fixtures: fifteen STL, fifty-one 3MF, and three STEP files. The five binary STL files, seven generated ASCII STL byte streams, and fifty-one 3MF packages are deterministic generator outputs; thirty-five 3MF, four binary STL, and four ASCII STL inputs are intentional rejections. `FIX-MESH-039` is a separately authored analytic ASCII STL. Private shop/customer/controlled data, credentials, signing material, and private validation artifacts remain prohibited from the repository and public CI
- **Last completed task/checkpoint:** Desktop contract v9 preserves the prior eight commands and adds one complete bounded catalog-draft save command. The request carries exact settings/catalog expectations, all five resource collections, and a reason, but no selections, actor, time, profile, path, storage, policy, or audit authority. The native adapter supplies actor/time/profile evidence, enforces per-kind limits before typed mapping, preserves matching immutable source evidence across equivalent decimal formatting, rejects changed bytes under a reused version, runs off the UI task, and returns a path-free snapshot. Controlled save/reopen and active-selection downgrade pass; ordinary startup remains identity-unavailable and the WebView invokes neither catalog command. See `06-quality/use-2-persistence-validation.md`
- **USE-2 status:** In Progress. Durable USD rate/pricing drafts, one starter bundle, bounded multi-entry catalog/selection snapshots, and content-minimized authorization decisions persist through typed boundaries. Contract v9 completes the native catalog-draft save seam, but ordinary startup lacks authenticated identity and the WebView has no editor. Stored drafts or active-for-proposals values are not estimate authority, and rate/pricing still requires current-session confirmation. The category/list/detail editor, authenticated host identity/roles, reviewed shipped activation policy, broader recovery/protected-storage, accessibility, and three-OS evidence remain.
- **Work in progress:** The first category/search/list/detail catalog browser is implemented read-only. The usable-estimator sequence now adds governed successor editing and explicit save readiness without adding resource forms to Estimate or weakening contract-v8/v9 identity boundaries. USE-3 exact-STEP stock/material proposals, USE-4 coarse process/runtime proposals, and USE-5 workflow simplification follow. TASK-004 format, tolerance, representative-corpus, containment, accessibility, installation/signing, and GUI-1 fixture review remain parallel work. The GUI has no enabled multi-entry editor, automatic stock/material/runtime proposal, estimate/model persistence, supported importer, viewer, signed package, or production approval.
- **Next task:** Extend the read-only catalog browser into governed Draft successor editing with explicit local dirty state, validation, reason, and a persistent Save area. Keep Save visibly unavailable until a trusted native identity source exists; do not add a WebView identity field or activation shortcut. Then begin the versioned USE-3 exact-STEP rectangular stock-envelope proposal contract and analytic model-sensitive tests. No user input is needed for UI structure or USE-3 mechanics; shop-approved role assignments, numeric values, and policy review remain TASK-007/USE-6 gates.
- **Build status:** Exact OCCT `V8_0_0` commit `d3056ef80c9668f395da40f5fd7be186cae4501f` constructs on evidenced Apple Silicon, Ubuntu 24.04 x86_64, and Windows Server 2022/Visual Studio 2022 x64 configurations. Native Linux run 33546682993 and native Windows run 33546698680 pass at exact commit `7f2c378`; both assemble/re-verify new runtimes, inspect 24 binaries/23 OCCT dependency families, pass configured real-worker STEP/STL/3MF desktop-host smokes, and upload no developer binaries. The 2026-09-04 USE-1 working-tree build produces a 174 MiB unsigned arm64 `.app` with a re-verified 72-artifact package runtime; its GUI executable SHA-256 is `5e52f15c17ccd208862cfa9d051c1e68bad9696f0461dba3fd3303c92cda42fb`
- **Test status:** Current local validation passes 255 runtime tests plus one compile-fail doctest, 74 Python tooling tests, strict workspace/native-host/WASM Clippy, WASM test compilation, the offline release frontend build, all sixty-nine fixture hashes, 147-file planning validation, formatting, and diff hygiene. The explicit desktop-host feature run passes 42 tests with three configured native/package smokes intentionally ignored. Contract/native-host tests prove exact nine-command manifest/permission/capability/handler parity, authority-minimized v8/v9 requests, no WebView catalog invocation, identity-unavailable no-mutation, controlled five-kind save/reopen, active-selection downgrade, equivalent-decimal source preservation, immutable-version rejection, and record-limit rejection. The live macOS development app visually passes the Estimate and empty-catalog Settings layout at 1180×760; a populated-catalog visual interaction pass remains pending. Prior package/native smokes remain the latest package evidence. Three-OS Rust run 33548320853 passes exact commit `ea1f255`; current USE-2 work has local macOS evidence only.
- **Active technical debt:** Durable rate/pricing Settings are not automatically resolved for estimates and require fresh session confirmation; the GUI has no revision-history browser, backup control, governed catalog editor, or protected-storage policy. Contract v9 now supplies the native draft seam, but ordinary startup has no authenticated identity/role source, both catalog calls are unavailable, activation remains deny-all, and no catalog action is present in the GUI. The local hash/trigger checks are not external tamper protection or access control. Stock volume, material density/price, process, runtime, and operation costs remain manual because model geometry does not yet select or propose them. Pinned ISO 4217 registry validation, nonterminating rational-division rounding, rounding increments beyond decimal scale, and graph-wide evaluation/migration remain pending; canonical JSON remains an internal spike contract. Mesh comparison still lacks production tolerance/location policy, general DAG/material semantics, windowed/accessibility evidence, supported corpora, and representative machining fixtures.
- **Active blockers:** No shop rates block TASK-002 mechanics because production libraries start empty and tests are synthetic; TASK-007/M0.2 still requires real category/policy/calibration review, and ADR acceptance plus broader production work still require shop/security/legal inputs and representative fixtures
- **Open architecture decisions:** ADR-0001 through ADR-0014 are In Review
- **Supported model formats:** None supported for production. Optional Apple-Silicon, Ubuntu-x86_64, and Windows-x64 STEP/OCCT developer spikes plus bounded path-free ASCII/binary STL and direct/linear-component-chain/model-metadata 3MF comparison analyzers handle governed synthetic evidence only. First-class plan: STEP, STL, 3MF; experimental plan: IGES, OBJ
- **Geometry capabilities:** Kernel-neutral contracts, bounded control schema v2, asset-transport manifest v2, explicit verified-private-copy selection, exact Unix descriptor and Windows HANDLE direct delivery with unrelated-resource exclusion proof, worker-side identity/type/hash/length/quota verification, immutable byte-stream adapter input, cooperative acknowledgement with forced termination, Linux `x86_64`/`aarch64` parser socket/process syscall denial, deny-by-default authorization/audit seams, root-identity-bound capability resolution, a consumed already-open grant, private per-job staging, immutable output claiming, poll/post-exit aggregate workspace-output inspection and cleanup, governed storage ports, and a mandatory worker resource profile are implemented. TASK-004 analyzes bounded already-authorized ASCII/exactly-framed binary STL v3 into unresolved-unit mesh evidence and validates bounded in-memory 3MF v8 ZIP/OPC packages with explicit/default units, bounded well-known model metadata presence, either one direct mesh/build item or a bounded linear chain whose component objects each reference the immediately preceding object once, ordered leaf-to-build component transforms, complete chain provenance, retained Core vertex-index topology, and canonical millimetre measurements. The worker selects STEP, ZIP-framed 3MF, or STL from verified immutable bytes; STL/3MF emit additive `geometry-mesh-snapshot-v1` output while STEP retains `geometry-snapshot-v1`. Custom mesh deserialization validates format-owned algorithm/policy identities, warning/confidence derivation, topology implications, finite values, and conditional closed-measurement authority; the controlled decoder also binds the authorized source hash and reference variant. The common analyzer uses format-owned topology identity: 3MF indices versus exact canonical coordinate identity for current indexless STL. Every successful mesh result exposes topology policy v1 and not-applied welding status. All mesh paths expose separately versioned exact-predicate self-intersection state and categorical confidence/reasons: validated unit-resolved mesh reaches only `Low`; unresolved units, topology defects, detected intersection, or indeterminate coplanar overlap require `NeedsReview`; detected/indeterminate states withhold volume and centroid. Successful fixture schema v4 enforces that ceiling and withholding policy with explicit three-state intersection evidence. Persisted packages cover all six Core unit declarations, the normative omitted-unit default, one model-metadata layout, a two-link linear chain, one split-index seam, thirty-five rejected 3MF cases, four rejected binary cases, and four rejected ASCII cases. The forty-three persisted negative cases pin eighteen exact sanitized diagnostic classes and no geometry evidence, including duplicate/missing/external model StartPart relationships and duplicate/missing/wrong model content types. Negative-determinant interpretation remains unsupported until its orientation/volume-sign behavior is governed. GUI-4 intake computes a size-bounded SHA-256 fingerprint only after authorization/audit and rewinds the same open grant for worker consumption; worker verification remains independent. Both exact-B-rep and mesh controlled outputs have validated source-bound application decoders. Contract v4 content-minimizes that evidence into a path-free tagged desktop result, preserves STL source coordinates versus 3MF canonical millimetres, and keeps mesh draft estimating unavailable. Configured Ubuntu x86_64 and Windows x64 evidence proves the pinned native adapter through real STEP/STL/3MF desktop-host services; only Linux applies the parser syscall boundary. The Ubuntu Debian checkpoint additionally proves extracted package-resource runtime verification and real retained-session STEP analysis. Production mesh welding/tolerance/location policy, broader supported general-DAG/material and representative malformed entity/geometry evidence, general filesystem containment, hard storage quotas, hard macOS memory, macOS/Windows parser egress denial, hostile macOS descendants, Windows/macOS package integration, windowed interactive Linux/Windows acceptance, corpus review, and production support remain open
- **Runtime capabilities:** No runtime estimator or proposal is implemented. USE-2 persists one typed draft coarse-runtime input profile only; six estimation levels and validation structure remain documented plans
- **Advanced capabilities:** None implemented. Route optimization, capacity/opportunity, uncertainty, revision/PMI/coverage, feature allocations, learning, CAM reconciliation, availability, sourcing, and bid priority are planning artifacts only
- **Current fixtures:** `FIX-MESH-001/002` cover closed/open synthetic ASCII STL cubes; `FIX-MESH-003` is the reproducible binary encoding of the closed cube; `FIX-MESH-004`–`013` cover direct and linear-component 3MF transforms, every Core unit/default, and bounded model metadata; `FIX-MESH-014`–`031/034`–`036/050`–`057/060`–`065` are thirty-five rejected 3MF packages; `FIX-MESH-032/033/037/038` are four rejected binary STL derivatives; `FIX-MESH-039` is an analytic self-intersecting ASCII STL; `FIX-MESH-040`–`043` are four rejected ASCII STL streams; `FIX-MESH-044`–`046`, `FIX-MESH-058`, and `FIX-MESH-066` are five successful isolated OPC variations; `FIX-MESH-047/048` isolate reversed winding and a non-manifold shared edge; `FIX-MESH-049` persists coplanar `indeterminate` intersection; `FIX-MESH-059` persists an equal-coordinate/distinct-index 3MF open seam under parser v8; `FIX-MESH-060`–`062` persist duplicate, missing, and external model StartPart rejection; and `FIX-MESH-063`–`065` persist duplicate, missing, and wrong model content-type rejection. Every success uses schema-v4 three-state self-intersection/confidence expectations and every rejection has a typed schema-v1 no-evidence expectation. `FIX-STEP-001` is a deterministic OCCT-generated AP214 10 mm cube; `FIX-STEP-002` contains an intentionally invalid entity; `FIX-STEP-003` is a manually authored AP214 faceted rectangular prism with independent analytic 12 × 8 × 5 mm ground truth, 392 mm² area, 480 mm³ volume, and `(6, 4, 2.5)` mm centroid. All hashes reproduce and native measurements pass where success is expected; formal geometry/security review, broader CAD-tool independence, production mesh tolerance, and licensing remain open
- **Calculation capabilities:** CALC-001/003/005 and CALC-007–018 foundations, with CALC-009 limited to exact division; empty configurable rate cards; scoped/effective/approved resolution; unavailable/blocked ambiguity states; versioned pricing/rounding; synthetic EX-01/03/12 exact itemized traces; pinned replay; typed money/currency/units; graph validation; value states; provenance/version wrappers; canonical snapshots; and a headless session-only application composition over reviewed provisional geometry/manual inputs/rates/policy are implemented as a reversible spike
- **Nearest testable GUI:** The bounded GUI-5 Apple-Silicon developer slice already selects local STEP, runs real OCCT, collects explicit review/manual inputs, and renders a deterministic trace. USE-1 makes upload/analyze primary and moves reusable configuration behind Settings; USE-2 now restores immutable USD rate/pricing plus one optional material/offer/stock/machine/runtime starter bundle after restart with confirmations cleared. The result is not yet a low-effort model-derived manufacturing estimate: the saved resource bundle is not applied, so stock, material, process, runtime, and most costs still require manual entry while models and estimates remain session-only. The app remains unsigned/unnotarized, unsupported, and without a 3D viewport; mesh estimating remains unavailable.
- **Files likely next:** `apps/estimator-desktop/src/web.rs` and `styles.css` for governed Draft successor form state and disabled-reason/save presentation, followed by a new application/domain USE-3 stock-proposal service and exact-STEP model-sensitive fixtures with adoption/audit plumbing into the retained draft session. TASK-003/004 format, containment, package, legal, and cross-platform evidence continues in parallel. The verified native runbook and evidence remain in `apps/estimator-desktop/README.md`, `06-quality/task-003-validation.md`, `06-quality/task-004-validation.md`, `06-quality/gui-5-validation.md`, and `06-quality/linux-desktop-package-validation.md`

## Commands currently available

```sh
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace --all-targets --locked
cargo test --workspace --doc --locked
cargo clippy -p partprobe-estimator-desktop-ui --target wasm32-unknown-unknown --locked -- -D warnings
cargo clippy -p partprobe-estimator-desktop --features desktop-host --all-targets --locked -- -D warnings
(cd apps/estimator-desktop && trunk build --release --locked true --offline true)
python3 scripts/check_planning.py
python3 scripts/hash_fixtures.py
python3 scripts/generate_3mf_fixture.py
python3 scripts/generate_ascii_stl_topology_fixtures.py
python3 scripts/tests/test_native_tooling.py
python3 scripts/build_occt.py --source /approved/local/occt-source --build /private/tmp/partprobe-occt-build --install /private/tmp/partprobe-occt-install
python3 scripts/assemble_native_runtime.py assemble --occt-root /approved/local/occt-install --worker /approved/local/partprobe-geometry-worker --build-manifest /approved/local/occt-build/partprobe-occt-build-manifest.json --output /approved/local/partprobe-native-runtime
python3 scripts/assemble_native_runtime.py verify --runtime-root /approved/local/partprobe-native-runtime
python3 scripts/verify_native_runtime_links.py --runtime-root /approved/local/partprobe-native-runtime  # Linux
python3 scripts/verify_native_runtime_links.py --runtime-root C:/approved/runtime --dependency-tool C:/approved/VS/bin/dumpbin.exe  # Windows
PARTPROBE_NATIVE_RUNTIME=/approved/local/partprobe-native-runtime PARTPROBE_GEOMETRY_WORKSPACE=/private/tmp/partprobe-gui-worker cargo test -p partprobe-estimator-desktop --features desktop-host gui5_configured_worker_runs_real_step_through_retained_estimate_session --locked -- --ignored --nocapture
git diff --check
git status --short --branch
```

## Read these first

1. [AGENTS.md](../AGENTS.md)
2. [Documentation index](INDEX.md)
3. [Decision log](08-decisions/decision-log.md)
4. [Requirements matrix](03-requirements/requirements-matrix.md)
5. The requirement, ADR, architecture, domain, UX, and quality files relevant to the task
