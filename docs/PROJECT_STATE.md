# Project State

> **Status:** In Review
> **Last updated:** 2026-09-13
> **Related requirements:** All
> **Related ADRs:** ADR-0001–ADR-0014
> **Open questions:** OQ-001–OQ-050
> **Dependencies:** M0.2 evidence and shop review
> **Supersedes:** None

- **Current phase:** Phase 0 — Discovery and Validation
- **Current milestone:** M0.2 evidence and decision closure; TASK-001 and TASK-002 are validated on three OSes, while TASK-003 and TASK-004 are In Progress
- **Product readiness:** Pre-alpha engineering foundation. A bounded Apple-Silicon internal developer GUI runs end to end. USE-2 has schemas v1-v4 for rate/pricing, starter resources, multi-entry catalog/selection, and append-only authorization decisions. Desktop contract v12 has twelve exact commands and remains path-free and geometry-free. The Estimate workspace can generate a model-sensitive, session-only stock/material/coarse-runtime proposal from one-solid exact STEP evidence plus the persisted starter resource draft; an estimator reviews combined analysis and proposal evidence, accepts named coarse exclusions, enters quantity, and explicitly records zero additional test pieces when applicable before calculation. The exact developer viewer configuration can request and retain the selected STEP's governed display derivative, hand it directly to the same-window native indexed renderer, provide four keyboard-operable standard views, and show the matching proposal blank under documented display-only centered/source-axis placement. These are developer/live-test bridges, not a supported importer, CAM result, complete manufacturing estimate, approved quote, or production authority. Catalog edit/activation remains identity-unavailable and deny-all. The macOS app remains unsigned/unnotarized and OCCT redistribution is not approved; release acceptance remains absent
- **Current branch:** `main`; prior TASK-002/TASK-003 branch histories are consolidated and obsolete local/remote task branches are deleted
- **Repository visibility:** Public as of 2026-08-12 so hosted CI can continue. The public source and workflow boundary contains only project code/documentation and sixty-nine governed project-authored synthetic CAD fixtures: fifteen STL, fifty-one 3MF, and three STEP files. The five binary STL files, seven generated ASCII STL byte streams, and fifty-one 3MF packages are deterministic generator outputs; thirty-five 3MF, four binary STL, and four ASCII STL inputs are intentional rejections. `FIX-MESH-039` is a separately authored analytic ASCII STL. Private shop/customer/controlled data, credentials, signing material, and private validation artifacts remain prohibited from the repository and public CI
- **Last completed task/checkpoint:** The internal contract-v12 Mac build now self-starts from its verified embedded runtime, owns a private temporary worker workspace, and requires no launcher environment or viewer process flag. First run can install, confirm, and immutably save the Huntsville test profile in one action; later runs use the saved revision with one session confirmation. Eligible STEP analysis prepares inputs automatically, combines the two required evidence reviews, and offers a separate explicit-zero action for additional test pieces. The native renderer now preserves normals for higher-contrast lighting and draws the exact matching proposal blank as translucent amber stock under `proposed-stock-display-placement-v1`. A complete direct-launch cube rehearsal reached a USD 236.75 internal result and visibly showed model plus stock. Desktop contract v12, geometry measurement, calculation formulas, persistence schema, and authority boundaries remain unchanged. See `06-quality/use-3-stock-proposal-validation.md`, `06-quality/gui-5-validation.md`, and `06-quality/vis-1-renderer-validation.md`.
- **USE-2 status:** In Progress. Durable USD rate/pricing drafts, one starter bundle, bounded multi-entry catalog/selection snapshots, and content-minimized authorization decisions persist through typed boundaries. Contract v9 completes the native catalog-draft save seam, but ordinary startup lacks authenticated identity. The WebView now shows the intended successor boundary and persistent save status but cannot stage or submit edits. Stored drafts or active-for-proposals values are not estimate authority, and rate/pricing still requires current-session confirmation. Enabled validated successor fields, authenticated host identity/roles, reviewed shipped activation policy, broader recovery/protected-storage, accessibility, and three-OS evidence remain.
- **Work in progress:** VIS-2 reaches the developer desktop renderer through the bounded source/analysis-bound native chain with keyboard controls, a meaningful text equivalent, lit model normals, and a display-only matching proposal blank. This is partial accessibility and stock-review evidence, not screen-reader or cross-platform acceptance, standard-stock resolution, availability, editable stock authority, or CAM. Remaining VIS-1 gates are explicit acceptance or replacement of Tauri's feature-gated child-WebView API, device-loss/non-GPU fallback behavior, full screen-reader/accessibility work, broader HiDPI/representative performance, and Windows/Linux package behavior. Multi-entry catalog authority, standard stock, availability, detailed routing, CAM-calibrated runtime, unmodeled adders, model/estimate persistence, signing, and supported import remain open.
- **Next task:** Implement an explicit GPU/device-loss fallback and finish screen-reader/keyboard acceptance. Then introduce immutable governed stock-candidate editing rather than changing the display-only centered proposal in place. In parallel, replace the starter-Draft bridge with governed catalog selection/adoption and advance standard-stock/availability, detailed process/runtime, and manual-addition handling.
- **Build status:** Exact OCCT `V8_0_0` commit `d3056ef80c9668f395da40f5fd7be186cae4501f` constructs on evidenced Apple Silicon, Ubuntu 24.04 x86_64, and Windows Server 2022/Visual Studio 2022 x64 configurations. Linux/Windows remain earlier ABI-v3 evidence. On 2026-09-13 the current contract-v12 Apple-Silicon `viewer-spike` `.app` rebuilt at 203 MiB as an unsigned arm64 bundle, passed complete 72-artifact embedded-runtime verification, and passed the retained-session real STEP smoke, end-to-end proposal/adoption smoke, and source-bound display-scene retention against the package-contained runtime. GUI executable SHA-256 is `da508da1e20d3ae4a6549be82254a4311bec89171ff36ab323c7711ed86915c2`; embedded worker SHA-256 is `d27e4d7c0520d91abd76585d9f2eb4417663f06184f781714f74f79e8d8ec939`. A direct launch with no environment or process flags selected the governed 10 mm cube through the native picker, returned exact 10 × 10 × 10 mm, 600 mm², and 1,000 mm³ OCCT evidence, automatically prepared a 16.4 × 16.4 × 16.4 mm proposal, completed the streamlined reviews, and calculated USD 236.75. The same-window Metal view visibly showed shaded source faces inside the amber proposed stock. The optional launcher now only preflights the same unchanged package.
- **Test status:** Current local validation passes 310 default-workspace all-target tests, including the compile-fail contract; the `viewer-spike` host lists 60 tests and passes 55 ordinary cases with five configured native/package smokes ignored by default. The renderer passes nine ordinary tests with one GPU smoke ignored by default; that Metal smoke also passes explicitly after the lighting/stock change. Desktop contract/UI suites pass 10/13 tests. Strict workspace, viewer-feature, and WASM Clippy; the offline release frontend build; all 74 Python tooling tests; 151-file planning validation; formatting; diff hygiene; complete embedded-runtime verification; both package-contained real-STEP smokes; and configured scene retention pass. Live direct-launch inspection covers native selection, exact OCCT cube analysis, automatic model-sensitive proposal, saved-profile reuse, combined reviews, explicit-zero input, calculation, same-window navigation, four views, resize, the path-free text equivalent, shaded model faces, and visible proposed stock. Three-OS Rust run 33548320853 remains evidence for exact commit `ea1f255`; current USE-2/3, ABI-v4, display-ABI-v1, and VIS-1/2 changes have local macOS evidence only.
- **Active technical debt:** Durable rate/pricing Settings require fresh session confirmation; the GUI has no revision-history browser, backup control, enabled catalog editor, or protected-storage policy. Contract v9 supplies the native draft seam, but ordinary startup has no authenticated identity/role source, both catalog calls are unavailable, activation remains deny-all, and the visible successor action is disabled. The local hash/trigger checks are not external tamper protection or access control. Contract v10 exposes and adopts a session-only model-sensitive proposal, but only for one-solid exact STEP plus one starter Draft: it does not resolve standard stock/availability, orientation, detailed process/fixtures/tools/outside operations, CAM-calibrated runtime, administration/overhead, or risk. Those omissions remain explicit rather than silently estimated. ABI-v4 evidence is local Apple-Silicon only. Pinned ISO 4217 registry validation, nonterminating rational-division rounding, rounding increments beyond decimal scale, and graph-wide evaluation/migration remain pending; canonical JSON remains an internal spike contract. Mesh comparison still lacks production tolerance/location policy, general DAG/material semantics, windowed/accessibility evidence, supported corpora, and representative machining fixtures.
- **Active blockers:** Production calibration still lacks approved shop rates, supplier offers, runtime studies, overhead/pricing policy, and representative estimator review. The Huntsville profile unblocks repeatable usability testing only; it is not deployment authority. TASK-007/M0.2, explicit acceptance or stable replacement of the VIS-1 child-WebView composition, device-loss/non-GPU fallback and complete screen-reader evidence, governed editable stock-candidate and standard-size/availability semantics, ADR acceptance, and broader production work still require engineering evidence plus shop/security/legal inputs and representative fixtures.
- **Open architecture decisions:** ADR-0001 through ADR-0014 are In Review
- **Supported model formats:** None supported for production. Optional Apple-Silicon, Ubuntu-x86_64, and Windows-x64 STEP/OCCT developer spikes plus bounded path-free ASCII/binary STL and direct/linear-component-chain/model-metadata 3MF comparison analyzers handle governed synthetic evidence only. First-class plan: STEP, STL, 3MF; experimental plan: IGES, OBJ
- **Geometry capabilities:** Kernel-neutral contracts, bounded control schema v2, asset-transport manifest v2, explicit verified-private-copy selection, exact Unix descriptor and Windows HANDLE direct delivery with unrelated-resource exclusion proof, worker-side identity/type/hash/length/quota verification, immutable byte-stream adapter input, cooperative acknowledgement with forced termination, Linux `x86_64`/`aarch64` parser socket/process syscall denial, deny-by-default authorization/audit seams, root-identity-bound capability resolution, a consumed already-open grant, private per-job staging, immutable output claiming, poll/post-exit aggregate workspace-output inspection and cleanup, governed storage ports, and a mandatory worker resource profile are implemented. TASK-004 analyzes bounded already-authorized ASCII/exactly-framed binary STL v3 into unresolved-unit mesh evidence and validates bounded in-memory 3MF v8 ZIP/OPC packages with explicit/default units, bounded well-known model metadata presence, either one direct mesh/build item or a bounded linear chain whose component objects each reference the immediately preceding object once, ordered leaf-to-build component transforms, complete chain provenance, retained Core vertex-index topology, and canonical millimetre measurements. The worker selects STEP, ZIP-framed 3MF, or STL from verified immutable bytes; STL/3MF emit additive `geometry-mesh-snapshot-v1`, while ABI-v4 STEP emits `geometry-step-analysis-v1` containing the unchanged `geometry-snapshot-v1` plus precise uninflated source-axis AABB extents. Custom result deserialization validates format-owned algorithm/policy identities, warning/confidence derivation, topology implications, finite values, source binding, and conditional measurement authority. The common mesh analyzer uses format-owned topology identity: 3MF indices versus exact canonical coordinate identity for current indexless STL. Every successful mesh result exposes topology policy v1 and not-applied welding status. All mesh paths expose separately versioned exact-predicate self-intersection state and categorical confidence/reasons: validated unit-resolved mesh reaches only `Low`; unresolved units, topology defects, detected intersection, or indeterminate coplanar overlap require `NeedsReview`; detected/indeterminate states withhold volume and centroid. Successful fixture schema v4 enforces that ceiling and withholding policy with explicit three-state intersection evidence. Persisted packages cover all six Core unit declarations, the normative omitted-unit default, one model-metadata layout, a two-link linear chain, one split-index seam, thirty-five rejected 3MF cases, four rejected binary cases, and four rejected ASCII cases. The forty-three persisted negative cases pin eighteen exact sanitized diagnostic classes and no geometry evidence, including duplicate/missing/external model StartPart relationships and duplicate/missing/wrong model content types. Negative-determinant interpretation remains unsupported until its orientation/volume-sign behavior is governed. GUI-4 intake computes a size-bounded SHA-256 fingerprint only after authorization/audit and rewinds the same open grant for worker consumption; worker verification remains independent. Both exact-B-rep and mesh controlled outputs have validated source-bound application decoders. The native-only VIS-2 manifest, deterministic `PPDSCENE` artifact frame, and controlled-output decoder bind source/analysis identity, layouts, framing, limits, and chunk hashes before producing nonserializable finite/index-safe renderer arrays. Worker request/response schema v2 preserves v1 and the authoritative analysis reference while adding one exact optional display request/reference. Additive OCCT display ABI v1 reparses the same authorized immutable STEP bytes after measurement, applies the exact requested profile, preserves face locations/orientation, and returns a ceiling-bounded flattened root mesh through explicit C++ ownership. The worker binds and emits that derivative to the exact source/analysis hashes; the supervisor claims separate fixed outputs under one aggregate quota; and the application retains only a source/analysis-bound decoded scene. The exact developer viewer configuration now requests that scene, verifies matching retained selection/analysis identity, and gives the nonserializable value directly to a checked indexed native renderer; source mode hides stock. Contract v12 preserves v10's three canonical-millimetre path-free STEP AABB values, v4's tagged mesh DTO, and v11's viewer workspace state while adding one enumerated standard-view command/result; it still exposes no display geometry and preserves application-enforced mesh estimate unavailability. Earlier configured Ubuntu x86_64 and Windows x64 evidence proves ABI-v3 STEP/STL/3MF desktop-host services, while current ABI-v4 AABB and display-ABI-v1 evidence is Apple-Silicon only; only Linux applies the parser syscall boundary. The Ubuntu Debian checkpoint additionally proves extracted package-resource runtime verification and real retained-session STEP analysis. Production mesh welding/tolerance/location policy, broader supported general-DAG/material and representative malformed entity/geometry evidence, general filesystem containment, hard storage quotas, hard macOS memory, macOS/Windows parser egress denial, hostile macOS descendants, ABI-v4/display-ABI Windows/Linux and package integration, windowed interactive Linux/Windows acceptance, corpus review, and production support remain open
- **Runtime capabilities:** One session-only developer proposal computes coarse setup/programming/load/quality times from the exact persisted Draft profile and volumetric cutting time from model-specific removed volume. It is explicitly not CAM, calibration, scheduling, or production runtime authority; the six production estimation levels remain planned.
- **Advanced capabilities:** None implemented. Route optimization, capacity/opportunity, uncertainty, revision/PMI/coverage, feature allocations, learning, CAM reconciliation, availability, sourcing, and bid priority are planning artifacts only
- **Current fixtures:** `FIX-MESH-001/002` cover closed/open synthetic ASCII STL cubes; `FIX-MESH-003` is the reproducible binary encoding of the closed cube; `FIX-MESH-004`–`013` cover direct and linear-component 3MF transforms, every Core unit/default, and bounded model metadata; `FIX-MESH-014`–`031/034`–`036/050`–`057/060`–`065` are thirty-five rejected 3MF packages; `FIX-MESH-032/033/037/038` are four rejected binary STL derivatives; `FIX-MESH-039` is an analytic self-intersecting ASCII STL; `FIX-MESH-040`–`043` are four rejected ASCII STL streams; `FIX-MESH-044`–`046`, `FIX-MESH-058`, and `FIX-MESH-066` are five successful isolated OPC variations; `FIX-MESH-047/048` isolate reversed winding and a non-manifold shared edge; `FIX-MESH-049` persists coplanar `indeterminate` intersection; `FIX-MESH-059` persists an equal-coordinate/distinct-index 3MF open seam under parser v8; `FIX-MESH-060`–`062` persist duplicate, missing, and external model StartPart rejection; and `FIX-MESH-063`–`065` persist duplicate, missing, and wrong model content-type rejection. Every success uses schema-v4 three-state self-intersection/confidence expectations and every rejection has a typed schema-v1 no-evidence expectation. `FIX-STEP-001` is a deterministic OCCT-generated AP214 10 mm cube; `FIX-STEP-002` contains an intentionally invalid entity; `FIX-STEP-003` is a manually authored AP214 faceted rectangular prism with independent analytic 12 × 8 × 5 mm ground truth, 392 mm² area, 480 mm³ volume, and `(6, 4, 2.5)` mm centroid. All hashes reproduce and native measurements pass where success is expected; formal geometry/security review, broader CAD-tool independence, production mesh tolerance, and licensing remain open
- **Calculation capabilities:** CALC-001/003/005 and CALC-007–018 foundations, with CALC-009 limited to exact division; empty configurable rate cards; scoped/effective/approved resolution; unavailable/blocked ambiguity states; versioned pricing/rounding; synthetic EX-01/03/12 exact itemized traces; pinned replay; typed money/currency/units; graph validation; value states; provenance/version wrappers; canonical snapshots; and a headless session-only application composition over reviewed provisional geometry/manual inputs/rates/policy are implemented as a reversible spike
- **Nearest testable GUI:** The fresh contract-v12 developer build at `target/debug/bundle/macos/PartProbe.app` is directly launchable and presents the exact STEP proposal/estimate workflow plus a clickable same-window **Model & stock** screen. A new install uses one **Install Huntsville test profile** action; a later launch uses one **Use saved profile** action. The primary path is choose STEP, analyze, confirm analysis, confirm the automatically prepared proposal, enter quantity, explicitly confirm zero additional test pieces when applicable, and calculate. The native Metal viewport shows shaded source geometry and the matching translucent proposed stock; the inspector and four standard views remain path-free and keyboard-operable. The app is unsigned/unnotarized, unsupported, session-only for models/results, and mesh estimating remains unavailable.
- **Files likely next:** Device-loss/non-GPU fallback and complete viewer focus/screen-reader work are next, followed by immutable governed stock-candidate editing; explicit child-WebView API acceptance/replacement remains a VIS-1 closeout item. Governed catalog selection/adoption, standard-stock/availability resolution, and detailed process/runtime/manual-addition handling follow. The Settings successor form stays identity-gated until native authentication/roles are designed. TASK-003/004 ABI-v4/display-ABI cross-platform, format, containment, package, legal, and fixture-review evidence continues in parallel. The verified native runbook and evidence remain in `apps/estimator-desktop/README.md`, `06-quality/task-003-validation.md`, `06-quality/task-004-validation.md`, `06-quality/gui-5-validation.md`, and `06-quality/linux-desktop-package-validation.md`

## Commands currently available

```sh
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace --all-targets --locked
cargo test --workspace --doc --locked
cargo clippy -p partprobe-estimator-desktop-ui --target wasm32-unknown-unknown --locked -- -D warnings
cargo clippy -p partprobe-estimator-desktop --features desktop-host --all-targets --locked -- -D warnings
(cd apps/estimator-desktop && trunk build --release --locked true --offline true)
python3 scripts/check_planning.py
python3 scripts/hash_fixtures.py
python3 scripts/generate_3mf_fixture.py
python3 scripts/generate_ascii_stl_topology_fixtures.py
python3 scripts/tests/test_native_tooling.py
python3 scripts/build_occt.py --source /approved/local/occt-source --build /private/tmp/partprobe-occt-build --install /private/tmp/partprobe-occt-install
python3 scripts/assemble_native_runtime.py assemble --occt-root /approved/local/occt-install --worker /approved/local/partprobe-geometry-worker --build-manifest /approved/local/occt-build/partprobe-occt-build-manifest.json --output /approved/local/partprobe-native-runtime
python3 scripts/assemble_native_runtime.py verify --runtime-root /approved/local/partprobe-native-runtime
python3 scripts/verify_native_runtime_links.py --runtime-root /approved/local/partprobe-native-runtime  # Linux
python3 scripts/verify_native_runtime_links.py --runtime-root C:/approved/runtime --dependency-tool C:/approved/VS/bin/dumpbin.exe  # Windows
PARTPROBE_NATIVE_RUNTIME=/approved/local/partprobe-native-runtime PARTPROBE_GEOMETRY_WORKSPACE=/private/tmp/partprobe-gui-worker cargo test -p partprobe-estimator-desktop --features desktop-host gui5_configured_worker_runs_real_step_through_retained_estimate_session --locked -- --ignored --nocapture
git diff --check
git status --short --branch
```

## Read these first

1. [AGENTS.md](../AGENTS.md)
2. [Documentation index](INDEX.md)
3. [Decision log](08-decisions/decision-log.md)
4. [Requirements matrix](03-requirements/requirements-matrix.md)
5. The requirement, ADR, architecture, domain, UX, and quality files relevant to the task
