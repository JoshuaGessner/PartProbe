# Cross-Platform Testing

> **Status:** Draft  
> **Last updated:** 2026-08-12
> **Related requirements:** REQ-NF-001, REQ-NF-003, REQ-NF-006, REQ-NF-018; TEST-008, TEST-011, TEST-012, TEST-019, TEST-023
> **Related ADRs:** ADR-0001–ADR-0006  
> **Open questions:** Minimum OS/GPU versions and signing identities  
> **Dependencies:** Platform spikes  
> **Supersedes:** None

The CI matrix targets current supported Windows x86_64, macOS arm64/x86_64 as policy determines, and Linux x86_64. Core Rust tests share one contract while retaining platform-specific evidence where primitives differ. Manual run 31555774851 adds Ubuntu 24.04 x86_64 exact OCCT construction, runtime assembly/host verification, internal dynamic-link closure, and configured desktop-host smoke evidence under the Linux parser filter. Windows run 31613259785 adds the equivalent Windows Server 2022/Visual Studio 2022 x64 construction, all 11 adapter and 16 worker/process tests, manifest-bound runtime assembly/re-verification, 24-binary/23-OCCT-family x64 PE closure, and configured STEP-to-USD-702 host smoke. Both native workflows upload no binaries and do not exercise a window or package; the separate Debian workflow contains no native runtime. macOS hard memory and hostile-descendant escape prevention, macOS/Windows parser egress denial, Windows hard file-write bytes, general filesystem confinement, Linux aarch64 native behavior, and native-integrated interactive Linux/Windows GUI/package behavior remain explicitly unsupported or unproven rather than inferred from another platform's result. Native geometry, WebView, GPU, font, file-dialog, printing/PDF, HiDPI, path/Unicode, long-path, permission, backup, process-crash, package/install/uninstall, signing, and update behavior require platform-specific integration evidence.

Use deterministic headless tests where possible plus physical/virtual smoke systems. Record OS, architecture, GPU/backend, WebView/runtime, locale, scale factor, and package type with failures. A green core build does not prove packaging or viewport viability.

The opt-in `Linux Desktop Package Evidence` workflow adds a deliberately narrower Ubuntu 22.04 x86_64 Debian-package gate. It builds with pinned developer tools, inspects and privately extracts the package without publishing it, and requires the exact Debian-contained desktop executable to remain alive for a bounded virtual-display smoke. Initial run 31610449968 produces the Debian package but stops in the requested AppImage phase; that phase also fetches moving helper inputs, so AppImage is deferred until exact sources/hashes are governed. The config names the exact reviewed icon files and the corrected workflow targets Debian only. Run 31612819644 passes the complete extracted-payload gate in 15m40s at `b6d6ecd`, recording the amd64 Debian metadata plus package/executable hashes and the expected ten-second survival. The external native runtime is absent, and the session reports unavailable accessibility/desktop-portal services, so this proves package construction, contained-executable integrity, and shell-process startup only—not STEP analysis, native-dialog/portal behavior, physical display/GPU behavior, signing, installation, or supported-distribution compatibility. See [Linux desktop package validation](linux-desktop-package-validation.md).
