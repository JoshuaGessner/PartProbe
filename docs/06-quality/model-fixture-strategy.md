# Model-fixture strategy

## Metadata

- **Status:** In Review
- **Last updated:** 2026-09-02
- **Related requirement IDs:** GEO-001–GEO-014, FEAT-001–FEAT-021, TEST-021–TEST-039, SEC-004
- **Related architecture decision IDs:** ADR-0002, ADR-0004, ADR-0005
- **Open questions:** What anonymized customer models can be contributed? Licensing policy for external samples? Artifact-store size budget?
- **Dependencies:** Controlled fixture repository/storage, expected-result schema, legal/release review
- **Supersedes / superseded by:** None / none

## Policy

Fixtures are versioned test data, not convenient sample CAD. Public fixtures must be legally redistributable, non-controlled, minimal, documented, hashed, and paired with reviewed expected results. The current bootstrap cubes are not approved for redistribution until the project license decision and review fields are resolved. Do not commit customer, defense, export-controlled, price, drawing, or identifiable RFQ data. Reviewed synthetic analytic fixtures become the authoritative baseline for measurement accuracy.

## Corpus layout and manifest

```text
fixtures/
  manifest.yaml
  models/<descriptive-source-name>.<ext>
  expected/<fixture-id-or-name>.json
  private/  # ignored; governed shop fixtures are never assumed distributable
```

The root manifest is the canonical registry and links each source to its expected file. It includes fixture ID/title, source SHA-256, origin/license/redistribution approval, sensitivity classification, format/schema/version, declared units, canonical units, intended representation, purpose/tags, expected worker outcome, resource ceiling, owner, reviewed date, and links to issue/requirements as those fields become applicable. Expected files identify analysis/feature profile and algorithm versions. A fixture update is a code-reviewable change, never a silent replacement.

Expectation schema version 4 encodes exact numeric values as decimal strings; every potentially missing measurement as an explicit `available`, `unavailable`, or `not_applicable` evidence state; explicit `not_detected`, `detected`, or `indeterminate` mesh self-intersection evidence; and categorical confidence with validated unique reason codes. Diagnostic codes explain unavailable evidence; absence never means zero. The schema rejects mesh records without triangle counts or self-intersection state, legacy boolean self-intersection values, confirmed unknown units, duplicate warnings/confidence reasons, mesh confidence above `Low`, negative area/volume, authoritative enclosed volume for a known open mesh, and enclosed volume or centroid for detected or indeterminate intersection. Detected/indeterminate states require matching `NeedsReview` confidence reasons and warnings; `not_detected` rejects those contradictory codes. Version 4 deliberately supersedes test-fixture-only v3 because a boolean could not preserve the runtime's coplanar `indeterminate` state. No production/customer records exist; every committed successful expectation is explicitly upgraded, v1–v3 files fail validation, and the failure-expectation schema remains separately pinned at v1.

Every successful mesh manifest record also pins `partprobe-mesh-topology-policy-v1`. Runtime evidence identifies `source_vertex_indices` for 3MF or `exact_source_coordinates` for STL and reports `welding_status: not_applied`. This evidence-contract expansion moves STL parsers to v3 and 3MF to v8 without changing schema-v4 expected measurements, fixture hashes, warnings, confidence, or accepted/rejected boundaries. Older v2/v7 evidence must not be inferred to contain welding-policy provenance.

## Initial suite

| Family | Mandatory cases | Primary validation |
|---|---|---|
| Analytic exact | cube, rectangular prism, cylinder, cone, tube, stepped turn profile, multi-body transform | units, volume/area/centroid/AABB/OBB and rotational indicator |
| STEP | AP203/214/242 single solid; named/color body; assembly; partial transfer; invalid shell | transfer report, attributes/provenance, exact measures |
| Mesh | watertight cube/cylinder at two resolutions; open/non-manifold/reversed/degenerate/self-intersecting inputs | mesh validation, unit prompt, approximate measurements/confidence ceiling |
| 3MF | all Core units/default, bounded linear component-chain/build transforms, metadata, unsupported extension, unsafe archive | unit/read behavior, ordered-chain provenance, and archive controls |
| IGES / OBJ | legacy surface/solid, missing assets, external-path attempt | secondary-format warnings and containment |
| Feature parts | through/blind hole, counterbore/countersink, pocket/open pocket, slot, boss, thin wall, turning profile, ambiguous blends | type/dimensions/evidence/confidence and visual mapping |
| Adversarial | malformed records, giant counts, recursion/compression bombs, invalid UTF/XML, worker fault injection | quotas, controlled failure, no UI crash |

`FIX-STEP-001` is a project-generated AP214 10 mm cube with declared millimetres, one solid, 600 mm² surface area, 1000 mm³ volume, and centroid `(5, 5, 5)` mm. The committed feature-gated generator uses the pinned OCCT 8.0.0 build and normalizes the volatile STEP header timestamp to `2000-01-01T00:00:00`; repeated generation produced SHA-256 `031304b3a6d9dd55a97b3329e7238286ccfdaa7f13030bbe6e5c4c5744fcc8a2`.

`FIX-STEP-002` is a small project-authored STEP envelope containing one intentionally invalid entity, SHA-256 `b0bb25521620d14798110de10dc78739e18d28cd160a10a16c4163a269ca1157`. Its separate failure-expectation schema version 1 requires recoverable `STEP_TRANSFER_FAILED`, no snapshot or output file, and removal of the staged input. It carries no geometry measurements or unit claims. This schema is additive and does not migrate or reinterpret schema-v4 successful-geometry expectations; unsupported failure-schema versions are rejected.

`FIX-STEP-003` is a project-authored ISO 10303-21 AP214 faceted B-rep rectangular prism. It is written directly rather than exported by OCCT, declares millimetres, and has analytic dimensions 12 × 8 × 5 mm, area 392 mm², volume 480 mm³, and centroid `(6, 4, 2.5)` mm. Its SHA-256 is `a3a2cceef68a98212a2b05ac376da747758cc360fb02085fee3f6db766dc2138`. Native adapter and supervised-worker tests reproduce those values within 0.000001, but `reviewed_date` remains unset until geometry and security reviewers accept the authoring/provenance record.

`FIX-MESH-001/002` exercise the bounded `partprobe-ascii-stl-spike-v3` comparison analyzer under explicit 65,536-byte and 1,000-triangle ceilings. The closed cube reproduces 12 triangles, 10 × 10 × 10 source-coordinate extents, 600 square source units, 1,000 cubic source units, centroid `(5, 5, 5)`, and no detected self-intersection; unresolved units hold confidence at `NeedsReview`. The open cube reproduces 10 triangles and 500 square source units while keeping volume and centroid unavailable and adds `OPEN_BOUNDARY` to its confidence reasons. `FIX-MESH-003` is a deterministic 684-byte binary encoding of the same closed analytic cube, generated from `FIX-MESH-001` and reproduced by a checked script; `partprobe-binary-stl-spike-v3` returns the same geometry, self-intersection, confidence, and unresolved-unit evidence.

`FIX-MESH-004` is a deterministic 1,072-byte 3MF Core/OPC package derived from the same reviewed triangles. It stores the 10 mm cube as coordinates `0..1` with explicit centimetre units and applies one build-item translation of `(2, 3, 4)` source units. `partprobe-3mf-spike-v8` therefore must report canonical 10 mm extents, 600 mm² area, 1,000 mm³ volume, translated centroid `(25, 35, 45)` mm, no detected self-intersection, and `Low` confidence with the mesh-representation-ceiling reason while retaining transform and indexed-topology evidence.

`FIX-MESH-005` is a deterministic 1,107-byte variant over the same leaf mesh. Object 2 references mesh object 1 exactly once with a positive-determinant X-scale of 2 and translation `(1, 2, 3)` source units; build item 2 then applies translation `(4, 5, 6)`. The governed component-then-build order yields canonical 20 × 10 × 10 mm extents, 1,000 mm² area, 2,000 mm³ volume, and centroid `(60, 75, 95)` mm. The parser must retain both object IDs and transforms, and must keep component absence distinct for `FIX-MESH-004`.

`FIX-MESH-006`–`011` persist the remaining Core unit declarations—micron, millimetre, metre, inch, and foot—plus the normative omitted-unit millimetre default. The generator converts the governed 10 mm source with 28-digit decimal arithmetic, omits the identity build transform, and fixes ZIP entry order, timestamp, permissions, compression, relationship/content-type parts, and XML bytes. Each package must reproduce 10 × 10 × 10 mm extents, 600 mm² area, 1,000 mm³ volume, and `(5, 5, 5)` mm centroid while distinguishing explicit declarations from the default. Together with `FIX-MESH-004`, all six Core declarations and the default are now committed, hashed, and paired with separate expectation identities. They remain multiple encodings of one analytic cube—not independent shape, exporter, or representative-part evidence—and do not themselves establish branching/DAG/union semantics, multiple build items, metadata/materials, desktop/worker integration, or production-importer support.

`FIX-MESH-012` adds three public synthetic, well-known Core model metadata entries (`Title`, `Application`, and `Description`) before the resources section, with `preserve=true` on one entry. The v8 parser keeps only total and preservation-request counts, emits `THREE_MF_METADATA_NOT_INTERPRETED`, and never returns names or values. The fixture reproduces the same 10 mm cube measurements and proves deterministic generation plus duplicate/name/boolean/limit failure behavior. It does not authorize metadata as geometry, a manufacturing requirement, or an estimating input and does not cover vendor namespaces/types, object/item metadata groups, materials, or source modification/preservation behavior.

`FIX-MESH-013` is a deterministic 1,124-byte two-link linear component variant. Object 2 references mesh object 1 with the `FIX-MESH-005` X-scale/translation; object 3 references object 2 with a Y-scale of 3 and translation `(1, 0, 2)` source units; the build then translates object 3 by `(4, 5, 6)`. The v8 parser must retain both ordered ID/reference/transform links and produce canonical 20 × 30 × 10 mm extents, 2,200 mm² area, 6,000 mm³ volume, and centroid `(70, 125, 115)` mm. Its explicit object and component ceilings bound chain depth. This fixture governs only a linear one-reference-per-object chain: branching, unions, repeated/non-immediate references, and general DAG semantics remain rejected; `FIX-MESH-014/015` pin two of those rejection boundaries without establishing general graph support.

`FIX-MESH-014`–`018` persist five deterministic public rejection packages: a component object with two references, an outer component that skips its immediate predecessor, object-level metadata, a StartPart relationship with parent traversal, and a second case-fold-equivalent model part. They are 1,111, 1,120, 1,095, 1,072, and 1,543 bytes respectively. Their schema-v1 failure expectations bind exact hashes to three `THREE_MF_UNSUPPORTED_MODEL_STRUCTURE` and two `THREE_MF_UNSAFE_PACKAGE` results and forbid snapshots, output files, or retained staged input.

`FIX-MESH-019`–`023` add a two-item build union, item metadata, vendor-defined model metadata, a 32 KiB highly compressible unused part, and a raw-header unsupported-compression declaration. They are 1,058, 1,097, 1,091, 1,253, and 2,108 bytes with SHA-256 values `c6f3f10abbca80996200f57df6d68b958e658fcf80d2bded095930d151a5556a`, `093eeb92c452b98c4fe1322b20fc8b0c79c6041853e88c73e84963895b35f070`, `feeb85b4b4a6f853f618e2ab276c8a1e7b5b1833066fd9b89a16a2d1504ef002`, `be26b03d1f4c4ff7a17b1dca965c6958c126f9c0099c0140c802f2b956998f82`, and `a8e8513186737e5aa1c0c3d06f8fa3930dbfa7ce6665e553b0c14b5bc56efd6a`. Their typed expectations pin three `THREE_MF_UNSUPPORTED_MODEL_STRUCTURE`, one `THREE_MF_ARCHIVE_LIMIT_EXCEEDED`, and one `THREE_MF_UNSUPPORTED_COMPRESSION` result.

`FIX-MESH-024`–`028` add a forward component reference, a build that leaves the final component object unused, a triangle material property attribute, a required extension, and an encrypted-entry declaration. They are 1,123, 1,105, 1,061, 1,102, and 1,072 bytes with SHA-256 values `f1bbab9f1184736a4e847657a312794dd6eceaf0be616821871b4c6b66152f3d`, `c3c5ea150fb5d64847d059685b9c19c3681b714827d1233d0712a1a1c04414a4`, `3654b4d40f73a8af0cb99f203b2b0d4e8f1b43179e3e92ad0344064419834bd4`, `f97ae44822515028967d3f7bdb3a91418855e8390857ef516f9413726e0cef29`, and `d4dc9572393cbd9172d53b4eaba666d9cab96b5efff7595de2df8fe027ee2883`. Their typed expectations pin three `THREE_MF_UNSUPPORTED_MODEL_STRUCTURE`, one `THREE_MF_UNSUPPORTED_REQUIRED_EXTENSION`, and one `THREE_MF_UNSAFE_PACKAGE` result. These packages contain no accepted geometry evidence and prove only parser-level failure behavior; supported general DAG/material semantics, alternate legal packages, broader malformed/resource-limit inputs, worker/desktop cleanup, and general malicious-package coverage remain open.

`FIX-MESH-029`–`031` persist absolute-path, backslash-containing, and directory ZIP entry names. They are 1,220, 1,221, and 1,168 bytes with SHA-256 values `85ac30dcf683c35055e0b5a05fbadff46102ef44221206823958cba3808e74d5`, `e91583770bc68c01b7f25862e62c533ed1b6e5943a75df4023601497f4ed88f3`, and `5dd5c8c56a73eb2f06eb417ba5c5b6ee395dc753db1cfc60e2675967885c7722`. Each pins `THREE_MF_UNSAFE_PACKAGE` before part decompression or geometry parsing. `FIX-MESH-032/033` are 683-byte truncated and 684-byte nonzero-attribute binary STL derivatives with SHA-256 values `90179eaaa95607b2d38d82251ee2856b82403872c4413ed48dca7786f813cae7` and `27aab9e23258b90e1fe75548efd6351ab69de29ad07fc992935a15901e61ed6a`; they pin `STL_INVALID_STRUCTURE` and `STL_UNSUPPORTED_ATTRIBUTE_DATA`. All five are public synthetic parser-level rejection evidence with no geometry result. General DAG/material semantics, alternate legal OPC layouts, broader malformed numeric/resource-limit inputs, worker/desktop cleanup, and representative malicious-package coverage remain open.

`FIX-MESH-034`–`036` persist malformed model XML, a forbidden document-type declaration, and a 17-entry package over the configured 16-entry ceiling. They are 1,072, 1,086, and 2,934 bytes with SHA-256 values `5d380642c97987879e496c1bf71b38e2a06a2177382fca14b3642db36a5f1a92`, `c9e508a02f3cf83db4afd0ca1f2e19a911bbc9280ccd9e46911bc2dbc18059d2`, and `abb35e4033e6fd3e577393a4ae450ca95bce663442092ced5cb313cbfc2320c9`; they pin two `THREE_MF_INVALID_XML` results and one `THREE_MF_ARCHIVE_LIMIT_EXCEEDED`. `FIX-MESH-037/038` are 684-byte binary STL derivatives containing an explicit quiet-NaN normal and a declared 1,001-triangle count over the 1,000-triangle ceiling. Their SHA-256 values are `02f3f921e156fb64430217e0d985cfde9bcbe580eefc4d9df3f9feb303d1f399` and `38349d654bc704abc0380ad3e8cfd3a45fd7330266cd14dd3b18cc0b918305e7`; they pin `STL_INVALID_NUMBER` and `STL_TRIANGLE_LIMIT_EXCEEDED`. All five remain public synthetic no-geometry parser evidence. Broader malformed entity/geometry, alternate legal OPC, worker/desktop cleanup, and representative malicious-input coverage remain open.

`FIX-MESH-040`–`043` persist four generator-defined adversarial ASCII STL byte streams: invalid UTF-8, a facet missing its third vertex, an empty solid, and one collinear triangle. They are 43, 120, 27, and 123 bytes with SHA-256 values `53b117ca3ae61779a158287188df630c42179ff1aaf4ebc84ad39a40a75741fc`, `5e895d55eb8520e9553508ea754f0a2a335aeaa0fc5e598db6505e2eb773f75c`, `cae5bfc3f6f573c0b068c554e6f065779f715d2624a4c69a557b38750f00cf3c`, and `082130ac066b1b87143b618c39a6269c5cffc013234d51fa9614c019a521f7c5`. Their schema-v1 expectations pin `STL_INVALID_TEXT`, `STL_INVALID_STRUCTURE`, `STL_EMPTY_MESH`, and `STL_DEGENERATE_TRIANGLE`, with no snapshot, output, or retained staged input. They run through content-based STL selection and add no accepted syntax, calculation change, parser-version change, worker/desktop evidence, or representative-corpus claim.

`FIX-MESH-044`–`046`, `FIX-MESH-058`, and `FIX-MESH-066` persist five positive OPC variations over the same translated 10 mm cube as `FIX-MESH-004`: a `.model` `<Default>` content-type mapping, an alternate `/3D/primary.model` StartPart with explicit `TargetMode="Internal"`, Store compression for every package entry, a standard internal package thumbnail, and the baseline `/3D/3dmodel.model` StartPart with an explicit `TargetMode="Internal"`. They are 1,062, 1,088, 2,108, 1,327, and 1,083 bytes with SHA-256 values `9483eefe0b2f39489b6ac19a17fea9b80bfd8040e6ac538e4e7cb095fa0220d4`, `6e6b281e971dd2871f06ed5ba9f62cff07f4d51efe515801137f05e821a52b54`, `16944c712b4c851c374f2a39d13b66fd5fc32ecebf570e342ccf24e192338198`, `40a575b863331e5bce4976a5f97b00959d6916fd4a8218cc65aa8dc937dda4cc`, and `0ae68b401d78b92e6405564e243e7011e5f437a5681243a42c7eecc59d04ae2b`. `FIX-MESH-058` adds exactly one `image/png` content-type override, one package-root thumbnail relationship with an explicit internal target, and one generator-authored 68-byte transparent 1 × 1 RGBA PNG at `/Metadata/thumbnail.png`; `FIX-MESH-066` changes only the baseline model relationship's optional target-mode declaration. The model bytes remain unchanged. This follows the [3MF Core specification's optional Package Thumbnail part and internal-target requirements](https://github.com/3MFConsortium/spec_core/blob/master/3MF%20Core%20Specification.md). Each fixture must reproduce 10 mm extents, 600 mm² area, 1,000 mm³ volume, and the translated `(25, 35, 45)` mm centroid with `Low` mesh confidence. These fixtures isolate already-accepted parser behavior and do not change that accepted package boundary; their current schema-v4 records differ only through the later test-contract migration. They establish only these five legal package axes—not arbitrary exporter compatibility, arbitrary extra-part preservation, broader OPC layouts, worker/desktop integration, representative geometry, or supported-importer acceptance.

`FIX-MESH-047` is a deterministic 1,486-byte derivative of `FIX-MESH-001` with exactly the first triangular facet reversed and its declared normal adjusted. SHA-256 `a22b5219977b9b8a3a520d617db86c9d6890cc0759d9289eb918c28c9c815052` binds the unchanged 12 triangles, 10 × 10 × 10 extents, and 600 square source units. The mesh remains manifold and watertight but is not consistently wound; self-intersection remains `not_detected`, confidence remains `NeedsReview`, and volume/centroid are unavailable with `INCONSISTENT_WINDING` evidence.

`FIX-MESH-048` is a deterministic 1,019-byte analytic ASCII STL containing two individually closed tetrahedra that share one edge. Its generic second-tetrahedron orientation prevents the exact-predicate detector from conflating the topology case with coplanar-overlap uncertainty. SHA-256 `670fd6d1725f589e5d2192dfb1be2910a8ad494a763a4339a158d57bc222895e` binds eight triangles, 2 × 4 × 4 extents, and surface area `6 + 2√3 + 2√5 + √11` (`17.2532540417602`) square source units. The analyzer reports non-manifold/open topology, self-intersection `not_detected`, `NeedsReview`, and unavailable volume/centroid. Both new fixtures preserve unresolved STL units and established parser-v2/detector-v1/confidence-v1 behavior; they do not establish exporter prevalence, production welding/tolerance, representative geometry, or worker/desktop support.

`FIX-MESH-049` is a deterministic 293-byte analytic ASCII STL containing two overlapping coplanar triangles. SHA-256 `0a0adf3740c1204a4b218ac0d5b4594a38c9b211584066599a7b99b86266a895` binds two triangles, 2.5 × 2.5 × 0 source-coordinate extents, and four square source units. Detector v1 returns `indeterminate`; unresolved units and the open boundary also keep confidence at `NeedsReview`, while volume and centroid remain unavailable. Its schema-v4 expectation proves that uncertainty is not collapsed into a false boolean. It does not choose a production coplanar, near-contact, or welding tolerance.

`FIX-MESH-050/051` are deterministic 1,108-byte and 1,106-byte component-cube 3MF derivatives with SHA-256 values `75f256020a7b6e092e215fd22e689ad4bcc66710868ba465e6179c1c8293c21d` and `ee76d45b021899cfbfb46cc0e2084e1383a0230fd21a9826e8bcad3425cd5646`. They isolate a component transform with linear determinant `-2` and `0`, respectively, and both pin recoverable `THREE_MF_UNSUPPORTED_TRANSFORM` with no geometry evidence. Core 1.4 permits negative-determinant transforms when the resultant shape preserves its volume sign; the reflected fixture therefore records a deliberately narrower PartProbe-v8 capability boundary, unchanged from v6 and not malformed/Core-invalid input. The singular case records the current fail-closed policy for a matrix Core says should not be singular or nearly singular. Accepting either case later requires an explicit parser-version and migration decision plus orientation/volume evidence; these fixtures do not establish exporter prevalence, worker/desktop behavior, or supported import.

`FIX-MESH-052`–`054` are deterministic 1,077-byte, 1,068-byte, and 995-byte direct-mesh 3MF derivatives with SHA-256 values `c9a8c233ae5c2181ca09692c7e9a874258ef2acfa5f65e48e45d461b915a6bfd`, `366b82b457c13940326ecdcc2aa36e278eab227466139adf984fd9995849ff42`, and `562ec8b042a58bf5f8647eb6f61645b7cb00b7bf105746b71a07f1557176733`. They isolate a `NaN` first-vertex coordinate, a repeated first-triangle index, and removal of all twelve triangle elements while retaining the governed eight vertices. Parser v8 pins `THREE_MF_INVALID_NUMBER`, `THREE_MF_DEGENERATE_TRIANGLE`, and `THREE_MF_UNSUPPORTED_MODEL_STRUCTURE`, respectively, with no geometry evidence. The structural parser rejects the empty triangle set before the analyzer's later empty-mesh branch, so `FIX-MESH-054` is not evidence for `THREE_MF_EMPTY_MESH`. A changed accepted/rejected boundary or diagnostic requires an explicit parser-version and migration decision. These fixtures are synthetic parser-level evidence, not exporter prevalence, worker/desktop integration, representative malformed geometry, or supported import.

`FIX-MESH-055`–`057` are deterministic 1,073-byte, 1,073-byte, and 1,316-byte direct-mesh 3MF derivatives with SHA-256 values `f37532833aca7dbaf725237b4271818adcf0ceebc9df80d4df7cca214d0feb22`, `fd04720a89eb97f6e736c1c93d4f9e2b58d3967931833aa5fd499317d4221725`, and `833594cdc59861ba91d1fcd91df80800f299968da721dde64f8f2d68a9abe930`. They isolate removal of the first vertex's required `x` coordinate, one first-triangle index changed from `2` to `99` outside the eight-vertex table, and ninety-three additional vertices that make the governed mesh's 101st vertex exceed the configured 100-vertex ceiling. Parser v8 pins `THREE_MF_INVALID_XML`, `THREE_MF_UNSUPPORTED_MODEL_STRUCTURE`, and `THREE_MF_ENTITY_LIMIT_EXCEEDED`, respectively, with no geometry evidence. A changed accepted/rejected boundary, ceiling, or diagnostic requires an explicit parser-version and migration decision. These fixtures are synthetic parser-level evidence, not representative malicious input, exporter prevalence, worker/desktop containment, supported import, or a production resource budget.

`FIX-MESH-059` is a deterministic 1,063-byte derivative of the explicit-millimetre cube with SHA-256 `e496e7aec4b6bb3911ab5680c80384e451aedf885d27af4689c036682e190342`. It duplicates vertex 0 as index 8 at exactly the same coordinates and changes only the first triangle from index 0 to 8. The coordinates still describe 12 triangles with 10 × 10 × 10 mm extents and 600 mm² area, but the Core index graph has an open seam. Parser v8 therefore reports manifold but not watertight/consistently wound, detects non-topological contact under detector v1, returns `NeedsReview`, and withholds volume/centroid. V6 incorrectly reconstructed adjacency from coordinate equality and could report this package closed; current spike records are rebuilt/pinned to v8, and a historical v6 result must not be silently reinterpreted. The package intentionally violates the Core manifold-model condition and is warning-path validation—not a legal exporter-compatibility sample, production welding policy, worker/desktop evidence, or supported input.

`FIX-MESH-039` is a 1,109-byte project-authored ASCII STL containing two analytically defined, individually closed and consistently wound tetrahedral shells whose surfaces intersect. SHA-256 `ee0d75458135b961de2300b638d880f824a0bf69f6ae6beeb1c03ca0569a107f` binds eight triangles, 5 × 5 × 5 source-coordinate extents, and surface area `48 + 16√3` (`75.712812921102028`) square source units. The exact-predicate v1 detector returns `detected`; the mesh remains manifold/watertight/consistently wound but volume and centroid are explicitly unavailable. Confidence policy v1 returns `NeedsReview` with mesh-ceiling, unresolved-unit, and self-intersection reasons. `FIX-MESH-049` separately persists the coplanar `indeterminate` state and also withholds closed measurements. These cases do not establish production near-contact, coplanar-overlap, vertex-welding, tolerance, location-reporting, or performance policy.

Because OCCT generates and reads `FIX-STEP-001`, it proves deterministic project plumbing and analytic measurement reconciliation, not independent translator correctness. `FIX-STEP-003` removes that same-generator dependency for one simple solid, but it remains a project-authored manual file interpreted by one kernel—not a broad translator or production accuracy corpus. `FIX-STEP-002` adds one adversarial transfer/cleanup case, not a comprehensive malformed-input corpus. Release accuracy and containment still need independently reviewed CAD-tool exports plus broader malformed, alternate-schema, assembly, partial-transfer, and resource-limit cases.

## Ground truth and tolerances

Use analytic construction or a reviewed independent CAD/calculation reference. Document coordinate system, dimensions, unit, expected tolerance and whether the value is exact or approximate. A mesh fixture records triangle count, resolution and expected error bound; a finer mesh is not automatically an equivalent fixture. Feature ground truth is annotated with geometry refs, expected candidate state, deliberate ambiguities, known false positives and known false negatives.

## Governance

Fixture owners review additions with a geometry maintainer and security reviewer for malicious/controlled sets. Track provenance in source control; store larger approved assets in an access-controlled artifact location with immutable hashes. Malformed inputs may be encrypted/restricted if sharing them increases risk; CI fetches them only in controlled environments. Deleting or changing a baseline needs a reason, replacement or explicit retirement, and a regression-history record.
