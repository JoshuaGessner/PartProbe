# Dependency Record

> **Status:** In Review  
> **Last updated:** 2026-08-09
> **Related requirements:** REQ-NF-003, REQ-NF-010; TEST-001, TEST-020  
> **Related ADRs:** ADR-0001, ADR-0007
> **Open questions:** Organization-wide license approval and automated advisory/SBOM tooling  
> **Dependencies:** `Cargo.toml`, `Cargo.lock`, CI  
> **Supersedes:** None

This record covers dependencies introduced by TASK-001 and reused by TASK-002/003 plus the exact reversible GUI-3 desktop-spike dependencies. Exact versions are pinned in the workspace manifest and lockfile. Presence here records engineering review; it is not legal advice or a final organization-wide license approval. TASK-003 adds `cc` for its optional native bridge, `sha2` for controlled streaming source and independent derivative verification, Unix-only direct uses of the already-locked `libc` and `rustix`, the Bytecode Alliance capability-filesystem pair `cap-std`/`cap-fs-ext` for race-safe parent containment plus final-component no-follow behavior, Unix-only `close_fds` for the post-fork descriptor allowlist, and Windows-only direct use of already-locked `windows-sys` for the exact HANDLE-list launcher. GUI-3 adds Tauri/Leptos/WASM packages behind the desktop-host or wasm32 boundaries described below. GUI-4 connects those existing desktop packages to the already-reviewed application, domain, geometry, security, and Serde workspace crates; it adds no third-party package, version, license, native library, or network behavior. OCCT 8.0.0 remains an unapproved, local-only spike dependency and is not fetched, linked, or distributed by default.

## Direct dependencies

| Package | Exact version/features | Purpose | Alternatives considered | License/source | Maintenance and removal |
|---|---|---|---|---|---|
| `rust_decimal` | 1.42.1; `default-features = false`; `std`, `serde-with-str` | Authoritative fixed-precision money and canonical decimal-as-string serialization | bespoke scaled integer; another decimal crate | MIT; crates.io/upstream GitHub | Current release retrieved 2026-07-23; owner: calculation maintainers; replace behind `Money` and quantity wrappers if precision/overflow/replay gates fail |
| `serde` | 1.0.229; `derive` | Versioned DTO/snapshot serialization | handwritten encoders | MIT OR Apache-2.0; crates.io/upstream GitHub | Mature ecosystem dependency; owner: architecture maintainers; removal requires replacing derives on persisted contracts |
| `serde_json` | 1.0.151; defaults | Compact canonical snapshots, fixture expectations, and bounded internal worker protocol JSON for reversible spikes | custom canonical encoder; another canonical binary/text format | MIT OR Apache-2.0; crates.io/upstream GitHub | Owners: calculation and geometry maintainers; replace behind snapshot and worker-protocol boundaries before formats become external contracts |
| `cc` | 1.4.0; default features disabled; build dependency only | Invoke the platform C++17 compiler for the project-owned optional OCCT C ABI shim | handwritten cross-platform compiler discovery; CMake-driving crate; `cxx` bridge | MIT OR Apache-2.0; crates.io/rust-lang upstream | Owner: geometry maintainers; mature Rust project; removal replaces `build.rs` compilation without changing the C ABI |
| `sha2` | 0.11.0; default features disabled | Streaming SHA-256 verification of controlled source bytes before worker launch and independent verification/content addressing of claimed derivative bytes | platform hash commands; bespoke SHA-256; OS crypto APIs | MIT OR Apache-2.0; crates.io/RustCrypto upstream | Owner: security and geometry maintainers; pure-Rust portable API; removal occurs behind asset staging/document storage without changing stored digest semantics |
| `libc` | 0.2.189; default features disabled; direct dependency on Unix targets only | Supply `O_NOFOLLOW` for final-component protection and the narrow `setrlimit`/process-group primitives used before worker `exec` and during forced tree cleanup | hard-coded OS constants/declarations; `rustix` process feature inside pre-exec; a larger process crate; deployment service | MIT OR Apache-2.0; crates.io/rust-lang upstream | Owner: security/geometry/platform maintainers; already active transitively, so this adds no package; remove behind source-open and process-resource adapters if stable/async-signal-reviewed APIs provide equivalent behavior |
| `cap-std` | 4.0.2; default features disabled | Hold an open directory capability and resolve only relative asset paths while preventing traversal or symlink escape from that root | `canonicalize`/metadata prechecks; custom Unix `openat2` plus Windows handle traversal; pre-copy validation | Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT; crates.io/Bytecode Alliance upstream | Owner: security and geometry maintainers; maintained cross-platform capability API; remove behind `LocalAssetRoot` if the standard library or a future platform service provides equivalent race-safe containment |
| `cap-fs-ext` | 4.0.2; default features disabled; `std` only | Add final-component no-follow control to the capability-root open operation | accept a contained final symlink; race-prone metadata precheck; project-owned unsafe OS calls | Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT; crates.io/Bytecode Alliance upstream | Owner: security and geometry maintainers; version-aligned with `cap-std`; remove with the resolver or if `cap-std` gains equivalent final-link control |
| `rustix` | 1.1.4; Unix targets only; `default-features = false`; `std` | Atomically duplicate the granted descriptor close-on-exec and validate/change descriptor flags through typed syscall bindings | raw `libc::fcntl`; a broader process crate; non-atomic duplicate then flag | Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT; crates.io/Bytecode Alliance upstream | Owner: platform/security maintainers; already locked through capability crates, so this adds direct responsibility but no package; remove behind `platform` if the standard library exposes equivalent atomic descriptor operations |
| `close_fds` | 0.3.2; Unix targets only; default features disabled | Mark every non-stdio descriptor except the intended worker asset close-on-exec inside the async-signal-sensitive pre-exec hook | enumerate `/proc`; close descriptors directly; bespoke per-OS syscall loops; broad inherited descriptor set | MIT; crates.io/cptpcrd upstream | Owner: platform/security maintainers; small OS-specific surface with an intentionally inheritable sentinel regression; replace or remove if maintenance/advisory review fails or a standard/process API exposes an exact allowlist |
| `windows-sys` | 0.61.2; Windows targets only; default features disabled; `Win32_Foundation`, `Win32_Security`, `Win32_System_JobObjects`, `Win32_System_Pipes`, `Win32_System_Threading` | Bind the minimal Win32 HANDLE/process APIs needed for controlled pipes, exact inheritance, suspended launch, Job CPU/memory/process-tree containment, lifecycle, and sentinel regression | unstable standard-library process attributes; a broader process crate; raw handwritten declarations; external launcher/service | MIT OR Apache-2.0; crates.io/microsoft upstream | Owner: platform/security maintainers; already locked, so this adds no package/version; remove behind `platform` if stable Rust exposes exact handle-list, Job assignment, and equivalent lifecycle/resource control |
| `leptos` | 0.8.20; defaults disabled; `csr` only; wasm32 frontend only | Rust-authored reactive DOM UI while retaining semantic HTML/CSS behavior | Dioxus Desktop; Slint; Iced; egui | MIT; crates.io/leptos-rs upstream | Owner: desktop maintainers; GUI-3 spike only while ADR-0001 is In Review; remove with `apps/estimator-desktop` or replace behind `desktop-contract` if accessibility/platform gates fail |
| `serde-wasm-bindgen` | 0.6.5; wasm32 frontend only | Convert versioned desktop-contract DTOs at the Tauri IPC boundary | JSON text through JS; handwritten JS object mapping | MIT; crates.io/RReverser upstream | Owner: desktop maintainers; small bridge surface; remove if the bridge adopts generated bindings or another validated serializer |
| `wasm-bindgen` | 0.2.127; wasm32 frontend only | Narrow bindings for the packaged Tauri `invoke` function | generated Tauri JS bindings; handwritten external JavaScript module | MIT OR Apache-2.0; crates.io/rustwasm upstream | Owner: desktop maintainers; restrict use to the UI adapter; remove with the Leptos/WASM frontend |
| `wasm-bindgen-futures` | 0.4.77; wasm32 frontend only | Await typed native command promises without blocking the UI thread | callback-only bridge; custom promise polling | MIT OR Apache-2.0; crates.io/rustwasm upstream | Owner: desktop maintainers; remove with asynchronous WASM bridge replacement |
| `tauri` | 2.11.5; defaults disabled; `wry`; optional `desktop-host` feature | Native desktop window, command/event bridge, capability enforcement, packaged assets | Dioxus Desktop; Slint; Iced; bespoke `wry` host | Apache-2.0 OR MIT; crates.io/tauri-apps upstream | Owner: desktop/platform/security maintainers; feature-off in default workspace builds; replace behind `desktop-contract` if ADR-0001 gates fail |
| `tauri-build` | 2.6.3; optional build dependency under `desktop-host` | Generate desktop metadata and exact custom-command permission files from the declared app manifest | manually maintained permission schema; custom build script | Apache-2.0 OR MIT; crates.io/tauri-apps upstream | Owner: desktop maintainers; generated output is ignored and reproducible from source; remove with the Tauri host |
| `tauri-plugin-dialog` | 2.7.2; optional under `desktop-host`; no frontend dialog permission | Open the OS-native STEP picker in the Rust host using the plugin's asynchronous callback API | direct platform dialog APIs; `rfd`; frontend dialog permission | Apache-2.0 OR MIT; crates.io/tauri-apps plugins upstream | Owner: desktop/platform maintainers; host-only and command-gated; replace behind the native source-selection command if maintenance/security gates fail |

## Active dependency graph

`cargo tree --workspace --all-targets --edges normal,build` on 2026-08-09 showed:

- `rust_decimal` → `arrayvec 0.7.8`, `num-traits 0.2.19` → build dependency `autocfg 1.5.1`.
- `serde` → `serde_core 1.0.229`, `serde_derive 1.0.229` → proc-macro chain `proc-macro2 1.0.107`, `quote 1.0.47`, `syn 3.0.3`, `unicode-ident 1.0.24`.
- `serde_json` → `itoa 1.0.18`, `memchr 2.8.3`, `zmij 1.0.23`, plus Serde.
- GUI-3's target/feature-gated graph adds the Leptos reactive/DOM stack, the wasm-bindgen JS/WASM interop stack, Tauri core/runtime/Wry, the platform WebView/window/dialog stacks, and Tauri build-time schema/code-generation crates. On macOS this reaches AppKit/WebKit through Rust Objective-C bindings; Windows and Linux select their own WebView/window/native-dialog backends. The exact graph is locked but materially larger than the headless workspace, so automated license/advisory/SBOM and per-target package review remain release gates.
- `cc 1.4.0` → `find-msvc-tools 0.1.9` and `shlex 2.0.1`; all report MIT OR Apache-2.0. These execute only during builds, discover the platform compiler, and parse compiler flags.
- `sha2 0.11.0` → `cfg-if 1.0.4`, `cpufeatures 0.3.0` → `libc 0.2.189`, and `digest 0.11.3` → `block-buffer 0.12.1` / `crypto-common 0.2.2` → `hybrid-array 0.4.13` → `typenum 1.20.1`. `geometry-import` also references `libc 0.2.189` directly on Unix targets. These report MIT OR Apache-2.0-compatible expressions and add no intended network behavior.
- `cap-std 4.0.2` and `cap-fs-ext 4.0.2` share `cap-primitives 4.0.2`, `ambient-authority 0.0.2`, `fs-set-times 0.20.3`, `io-extras 0.19.0`, `io-lifetimes 2.0.4/3.0.1`, `ipnet 2.12.0`, `maybe-owned 0.3.4`, and target-specific `rustix 1.1.4`, `rustix-linux-procfs 0.1.1`, `winx 0.36.4`, and `windows-sys 0.59.0/0.60.2/0.61.2` families. Their supporting target graph adds `bitflags 2.13.1`, `errno 0.3.14`, `linux-raw-sys 0.12.1`, and version-matched Windows target crates. All report MIT, Apache-2.0, or the Apache-2.0-with-LLVM-exception alternatives recorded above.
- `platform` references already-locked `rustix 1.1.4` directly on Unix and adds `close_fds 0.3.2`, which reuses already-locked `cfg-if 1.0.4` and `libc 0.2.189`. Neither package adds intended network behavior; both are excluded from Windows builds.
- `platform` references already-locked `windows-sys 0.61.2` directly only on Windows with five narrow feature groups, adding JobObjects to the prior exact-launch surface. This does not add another binding version or intended network behavior; Unix builds exclude it.

Reported package licenses are MIT, MIT OR Apache-2.0, Apache-2.0 OR MIT, `memchr`'s Unlicense OR MIT, and `unicode-ident`'s combined MIT/Apache-2.0 and Unicode-3.0 expression. The Cargo 1.94 lockfile also records packages reachable through disabled optional features; the active graph above—not every lockfile entry—is the TASK-001 build surface.

## Security, build, and runtime review

- Project crates retain `unsafe_code = "forbid"` except `platform`, which sets `unsafe_code = "deny"` and permits unsafe only on individually attributed OS-boundary functions. Checkpoint 18 extends the existing Unix pre-exec exception with async-signal-safe resource/process-group syscalls and adds an attributed group-kill boundary; Windows adds attributed Job creation/configuration, suspended assignment/resume, and tree termination around the existing exact-launch functions. Four additional resource-inheritance probes remain test-only. This does not prove that dependencies contain no unsafe code; automated unsafe/advisory review remains a release gate.
- The headless default graph is Rust-only apart from the separately feature-gated OCCT bridge and showed no native linker dependency. GUI-3's feature-gated desktop graph deliberately links platform GUI/WebView libraries and executes Tauri/Leptos/WASM procedural and build tooling; platform system-package provenance and runtime versions therefore require three-OS package evidence.
- The selected libraries perform no intended runtime network access. Cargo requires registry/network access only to fetch uncached packages; `Cargo.lock` and `--locked` make resolution reproducible.
- GUI-3 does not enable a Tauri HTTP, shell, filesystem, opener, updater, upload, or frontend dialog permission. Its exact capability/app manifest and local-content CSP are regression tested. WebView and operating-system libraries remain complex native surfaces, and CSP/capabilities are not packet-level egress enforcement; target-specific network-denial evidence is still required.
- GUI-4 adds only direct links between existing workspace crates and reuses locked `serde` for WASM request envelopes, `serde_json` for contract tests, and `rust_decimal` in the desktop host for exact user-text parsing. Analysis/cancellation/evaluation accept host-issued opaque tokens rather than frontend paths, blocking work runs outside the UI thread, and returned DTOs remain path-free. These changes alter the purpose/direct dependency graph of existing packages but add no package, version, license, or intended network behavior.
- GUI-4's native worker adapter remains disabled unless the developer supplies explicit existing worker, workspace, and OCCT-root paths. Missing or invalid configuration fails closed; there is no package download, runtime discovery fallback, or silent in-process CAD parser.
- TASK-003 moves the already-reviewed Serde JSON package into the `geometry-import` runtime graph for bounded local protocol encoding/decoding; this changes its runtime purpose but adds no package or network behavior.
- The optional native bridge compiles only when `native-occt` is selected, requires an explicit local `PARTPROBE_OCCT_ROOT`, dynamically links shared OCCT libraries, and never downloads native code from `build.rs`. Developer construction also requires an explicit existing source checkout: `scripts/build_occt.py` validates the exact commit/tag/clean tree, applies fixed CMake options, records compiler/generator/source-tree provenance, and invokes content-addressed verification without downloading or deleting anything.
- SHA-256 is computed incrementally while copying into a create-new worker-local file. Default `sha2` allocation/OID features are disabled; CPU feature detection may select a supported backend and falls back to portable software.
- Local source opening is read-only and rejects a linked final component with `O_NOFOLLOW` on Unix or `FILE_FLAG_OPEN_REPARSE_POINT` plus a reparse-point attribute check on Windows. The Windows open also requests identification-only security quality of service. This uses safe standard-library APIs and no network access or project `unsafe`; parent-component authorization and containment remain application-service work.
- `LocalAssetRoot` deliberately opts into ambient authority only while opening an application-selected root, then performs relative capability-based lookups. Syntactic traversal and absolute paths are rejected before resolution; `cap-std` prevents parent symlinks from escaping the open root; `cap-fs-ext` rejects a linked final component. A root is not an authorization decision: actor, project, classification, record-state, audit, and root-selection policy remain application/security-service responsibilities.
- Standard-library `canonicalize` or `symlink_metadata` checks were rejected because a path component can change between validation and open. A custom cross-platform resolver was rejected because it would duplicate maintained Unix/Windows handle traversal and require project-owned `unsafe`, which workspace policy forbids.
- The capability crates and their OS abstraction dependencies use platform-specific code, including upstream-reviewed unsafe where required to call operating-system APIs; `unsafe_code = "forbid"` still applies to every PartProbe crate except the narrowly reviewed `platform` exception above. No runtime network behavior is intended. Automated advisory, source-integrity, and unsafe-code review remain release gates.
- Unix direct launch duplicates the already-open read-only grant with close-on-exec atomically. The child hook clears that flag only for the intended descriptor and uses `close_fds` to mark every other descriptor at or above 3 close-on-exec before `exec`; stdio remains under `Command` control. Windows direct launch creates controlled pipes, duplicates only the source and null stderr as inheritable, and passes exactly child stdin/stdout/stderr plus source through `PROC_THREAD_ATTRIBUTE_HANDLE_LIST`; parent pipe ends are non-inheritable. A PartProbe process-global mutex covers preparation through launch/drop of the temporarily inheritable source duplicate. Recursive tests on both OS families prove an unrelated inheritable resource is absent while source and stdio function. This does not coordinate arbitrary third-party code that deliberately launches a child with broad HANDLE inheritance, so such launchers remain prohibited inside the desktop process boundary.
- Every Unix worker launch also installs a hard CPU-time ceiling, regular-file-size ceiling, core-dump prohibition, and dedicated process group before `exec`; Linux additionally installs an address-space ceiling. Current macOS rejects the available hard memory rlimit candidates, and a hostile Unix child can leave its process group, so both remain explicit deployment gaps. Every Windows worker is created suspended, assigned to a configured Job with CPU, committed-memory, active-process count one, and kill-on-close limits, then resumed; forced termination targets the Job. Windows has no hard Job file-write byte ceiling, so the existing bounded output-claim validation remains necessary and aggregate-output sandboxing remains open.
- Default features are disabled for `rust_decimal` to reduce optional surface. Decimal overflow, scale, serialization, and cross-platform behavior are exercised by TEST-001 but still require representative golden estimates in TASK-002.
- CI pins the Rust toolchain and the checkout action commit. A future dependency gate must add automated advisory, SBOM, source-integrity, and full transitive-license evidence before release.
- TASK-003 may not add or distribute OCCT until an exact source/build/version, native transitive graph, LGPL-2.1-with-additional-exception obligations, notices/relinking approach, advisories, reproducible three-OS packaging, update owner, and adapter removal/replacement path are recorded and reviewed.

## Native candidate evidence — not distribution approval

| Candidate | Exact source/build | Local evidence | Remaining gate |
|---|---|---|---|
| Open CASCADE Technology | 8.0.0; official tag `V8_0_0`; commit `d3056ef80c9668f395da40f5fd7be186cae4501f`; tree `b3ffb8a91468845b63675057957209032b5806b1`; C++17 shared Release libraries; PCH/TBB/FreeType off; requested `TKDESTEP`, `TKShHealing`, `TKMesh` | Fail-closed Apple Silicon source construction passes from a fresh root and records CMake 4.3.4, Unix Makefiles, AppleClang compiler paths, options, and artifact hashes; strict native checks plus generated and manually authored analytic STEP measurements pass | Legal approval; Windows/Linux construction; cross-platform resolved inventory and license hashes; three-OS package fingerprints; notices/source/relink instructions; advisories/SBOM; signed artifacts |

The Apple Silicon build resolved 23 OCCT shared libraries: `TKernel`, `TKMath`, `TKG2d`, `TKG3d`, `TKGeomBase`, `TKBRep`, `TKGeomAlgo`, `TKTopAlgo`, `TKPrim`, `TKBO`, `TKShHealing`, `TKMesh`, `TKHLR`, `TKService`, `TKCDF`, `TKLCAF`, `TKCAF`, `TKV3d`, `TKVCAF`, `TKXCAF`, `TKDE`, `TKXSBase`, and `TKDESTEP`. This is observed spike evidence, not yet a cross-platform package manifest; each target artifact still needs a generated fingerprint and dynamic-link audit.

## Verification commands

```sh
cargo tree --workspace --all-targets --edges normal,build
cargo metadata --format-version 1 --locked
cargo test --workspace --all-targets --locked
python3 scripts/verify_native_step.py --occt-root /approved/local/occt-install
python3 scripts/tests/test_native_tooling.py
python3 scripts/build_occt.py --source /approved/local/occt-source --build /private/tmp/partprobe-occt-build --install /private/tmp/partprobe-occt-install
```
